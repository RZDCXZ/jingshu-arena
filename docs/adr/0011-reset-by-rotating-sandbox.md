---
status: accepted
---

# 通过轮换沙箱完成演示重置

访客重置演示时，系统创建一个全新的标准种子沙箱并把会话切换过去，旧沙箱立即失效并进入异步清理，而不是在原沙箱中逐表删除和重建。轮换事务在删除旧业务行前写入不依赖旧沙箱外键的持久清理任务；提交后先尝试删除该沙箱的私有 Blob，失败则由跨沙箱、幂等且可重试的工作器继续处理。业务行完成清理后只保留不含业务内容的最小删除回执与清理状态。该选择会让旧浏览器标签需要处理沙箱失效，也要求清理状态可重试；但它避免部分重置失败、旧请求写入新种子、丢失待删文件引用和超大删除事务造成的数据矛盾。
