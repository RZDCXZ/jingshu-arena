---
status: accepted
---

# 通过轮换沙箱完成演示重置

访客重置演示时，系统创建一个全新的标准种子沙箱并把会话切换过去，旧沙箱立即失效并进入异步清理，而不是在原沙箱中逐表删除和重建。清理先封锁访问并分批删除可重试的私有 Blob，再分批删除业务行，完成后只保留不含业务内容的最小删除回执。该选择会让旧浏览器标签需要处理沙箱失效，也要求清理状态可重试；但它避免部分重置失败、旧请求写入新种子、丢失待删文件引用和超大删除事务造成的数据矛盾。
