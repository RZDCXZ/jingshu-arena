---
status: accepted
---

# 使用 SSE 发送失效通知并以轮询降级

Web 服务端以约二十五秒的短连接按沙箱发送轻量失效通知，客户端收到后重新获取相关资源，并在连接正常结束后自动重连，而不在 SSE 中复制完整业务状态；SSE 被平台缓冲、提前终止或不可用时退化为短轮询和手动刷新。该方式适应 Cloud Function 默认三十秒超时，同时让多个角色及时看到彼此的操作，并把数据库和 API 保持为唯一权威。SSE 能力仍须在部署探针中验证，探针失败时轮询是正式退路而不是伪实时成功。
