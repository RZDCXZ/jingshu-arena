---
status: accepted
---

# 生产数据库使用前向 expand/contract 迁移

生产 schema 变更先增加与旧应用兼容的结构，再部署兼容代码和必要的数据回填，最后在后续版本移除旧结构；发布流程不依赖破坏性 down migration。部署失败时优先重新部署上一个兼容构建，并用新的前向迁移修复数据或 schema。该方式增加了阶段性重复结构和迁移规划成本，但避免应用回滚后发现旧字段已经被不可逆删除。
