---
status: accepted
---

# RLS 隔离沙箱，应用层约束角色与门店

Postgres RLS 使用每次事务显式设置的 `sandbox_id` 作为第二层隔离，缺少上下文时默认拒绝访问；应用权限层继续判断角色、门店和具体业务能力。该分工让数据库防止遗漏沙箱条件，又避免把复杂且变化频繁的四角色能力矩阵全部编码进 RLS。所有对象仍使用不可预测 ID 并执行对象级授权，不能把 RLS 或 ID 难猜当作单独安全边界。
