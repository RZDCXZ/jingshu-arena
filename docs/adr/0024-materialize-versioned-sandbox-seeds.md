---
status: accepted
---

# 为每个沙箱物化完整且版本化的合成种子

每个 Web 沙箱通过确定性生成器和批量 SQL，在单一幂等事务中物化独立的三店配置、十四个经营日历史、当前积压和主演示数据，而不与其他访客共享业务历史或以预计算指标替代记录。沙箱同时保存 schema 与种子版本，新部署只为新建或重置后的沙箱使用最新种子，并至少在旧沙箱剩余的二十四小时访问期内保持兼容。该方式增加了数据库行数和创建成本，因此热创建预算为三秒、活跃容量初始限制为二百个并设只读降级；换取的是简单可信的 RLS 隔离、真实筛选与导出、可复现测试以及不会被新部署静默改写的演示故事。
