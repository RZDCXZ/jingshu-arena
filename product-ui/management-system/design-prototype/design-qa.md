# 竞枢管理端设计 QA

- 状态：`current`（当前有效）
- 检查日期：2026-08-11
- 已验证契约版本：`product-ui/management-system/prod.md` 1.12
- 验证范围：[`22 — 单店经营看板与经营日下钻`](../../../.scratch/jingshu-basic-rc/issues/22-manager-dashboard.md)；保留 ticket 21 员工/排班/考勤和更早结论

## Ticket 22 `WEB-M01` 对照目标与结论

- 视觉方向继续使用选定源 `design/reference/selected-night-operations-console.png`，页面状态源为正式原型店长“经营看板”。应用内浏览器以精确 `1440 × 1024 CSS px` 和 `1024 × 768 CSS px` 打开正式原型与真实 Web → Hono → PostgreSQL 生产页；设备像素比为 1。
- 原始证据保存在 `design/evidence/ticket-22-manager-dashboard/source-formal-manager-dashboard-1440x1024.png`、`source-formal-manager-dashboard-1024x768.png`、`implementation-manager-dashboard-1440x1024-pass2.png` 与 `implementation-manager-dashboard-1024x768.png`。全屏同屏比较分别为 `comparison-full-1440x1024.png`（`2880 × 1024`）和 `comparison-full-1024x768.png`（`2048 × 768`）；主内容重点比较为对应的 `comparison-focused-main-*`。
- 两侧都保留“经营日标题与范围 → 四张核心指标卡 → 模拟营业额/双座位指标图 → 三块事实与证据面板”的信息层级。生产数据来自当前沙箱，因此数值和曲线与正式原型的代表性样例不同；海军蓝高密度壳层、青色信息反馈、青柠/琥珀状态、边框与圆角、正式品牌资产和 Phosphor 图标保持一致。
- `1440 × 1024` 下四张指标卡单行呈现，双图并列，下方订单维修、人员库存异常和最近经营记录同屏；`1024 × 768` 下指标卡转为两列、主图转为单列，与正式原型的响应式层级一致。两档生产页均有 `documentScrollWidth = viewportWidth`，没有页面级横向溢出或被裁切的主操作。
- 真实主路径依次验证当前经营日、最近七日、自定义 `2026-08-09` 至 `2026-08-10`、模拟营业额口径展开，以及最近七日指标卡下钻；下钻标题、行项目和范围均保留“棱镜旗舰店 · 2026-08-05 至 2026-08-11”。无数据状态提供“重新读取/返回当前经营日”，错误状态保留范围并提供“重试当前范围”。
- 干净生产标签在两档视口以及范围切换、口径展开、下钻和自定义范围之后 warning/error 均为 0。领域固定事实、真实 PostgreSQL 与 API 自动化覆盖 `06:00` 边界、十四日限制、片段退款、订单分母、两个座位分母、维修中位数/维护分钟、当前店长范围和其他角色拒绝。
- 第 1 轮 P2 已关闭：生产首版把报修和考勤也提升为顶部指标，形成六张过密卡片，同时下半屏在 `1440px` 下过早结束。最终恢复正式源的四卡主层级，把报修与考勤放回事实面板，并增高趋势图与下层密度；修复前后分别保存在 `implementation-manager-dashboard-1440x1024-pass1.png` 与 `implementation-manager-dashboard-1440x1024-pass2.png`。
- 第 2 轮 P2 已关闭：生产首版在 `1024px` 仍保留四卡单行和双图并列，正式源使用两列卡片和单列主图。最终在 `1180px` 断点切换两列指标与单列图表，合并比较确认结构一致。当前没有仍需处理的 P0、P1 或 P2 差异；P3 仅为权威沙箱数值/七日曲线与正式样例数值不同。

final result: passed

## Ticket 21 `WEB-M07/WEB-M08/WEB-M09` 对照目标与结论

- 应用内浏览器以精确 `1440 × 1024 CSS px` 对照员工工作区，以精确 `1024 × 768 CSS px` 对照八日排班、班次表单和考勤更正表单；设备像素比为 1。正式源、生产实现和同屏比较分别保存在 `design/source-ticket21-*`、`design/implementation-ticket21-*` 与 `design/qa-ticket21-*-comparison-*`，每组都使用同一视口、同一角色和同一任务状态。
- 员工页两侧保留固定门店范围、在职/停用状态、受保护人物和专用新建/编辑/停用动作；生产展示完整 16 人权威门店名单，正式源使用 5 行代表性数据。审查发现并关闭一项 P1：正式源原先把编辑员工编号和编辑班次窗口写死为单一样例，现均由当前行动对象带入；表格同时明确员工编号、角色、状态、固定门店、保护状态和依赖，周宁与许知远的编辑/停用在两侧禁用。
- 排班页两侧使用半小时语义、跨午夜标记、覆盖不足告警和班次专用表单；生产以八日时间轴呈现完整 48 条未来班次，正式源用单日样例表达相同层级。首轮 P1 已关闭：生产新建表单默认日期改为第四天可用时段，覆盖告警保持可见且提交按钮可用。审查又关闭一项 P1：生产原先仅用整日单元格承载满宽班次，现每个经营日绘制 48 个半小时刻度，班次按上海开始时刻定位、按真实时长绘制，跨午夜按两日拆段；同一员工重叠仍由服务端拒绝。
- 考勤页两侧始终并列保留原始事实与追加更正；真实沙箱完成店员模拟签到、切换店长、追加迟到更正并回读，更正原因、操作人和双时间可见，原始签到事实仍标为不可编辑。正式源的更正类型只保留迟到、缺勤与签退，交接快照、缺勤详情、取消班次和考勤更正分别使用具名专用弹窗，不提供任意状态字段；真实 PostgreSQL 另覆盖缺勤与签退更正以及错误事实类型拒绝且原记录不变。
- `1024 × 768` 下正式源与生产页面均为 `bodyClientWidth = bodyScrollWidth = 1024px`；生产班次弹窗为 `720 × 372.5px`，位于 `x=152px`、`y=197.75px`，考勤更正弹窗为 `720 × 425.28px`，均未裁切。专用表单焦点、关闭和返回触发器路径通过；新开正式源与生产标签页 warning/error 均为 0。
- 当前没有仍需处理的 P0、P1 或 P2 差异。P3 为有意的数据密度差：生产读取完整 16 人名单与八日权威排班，正式原型保留五行/单日代表性样例；页面职责、夜间高密度壳层、海军蓝表面、青色反馈、青柠主动作、琥珀覆盖告警、正式品牌资产和 Phosphor 图标一致。
- 真实 PostgreSQL/API 旅程确认背景员工可以从报修产品路径获得未关闭分派并触发停用依赖，总部只读汇总返回三店人数、未来班次、覆盖告警与考勤异常；跨店预览和命令均留下拒绝审计。完整 `JINGSHU_E2E_WEB_ORIGIN=http://127.0.0.1:3017 pnpm verify` 通过：321 个 Vitest 单元测试、2 个开发启动测试、164 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 53 个 Chromium E2E；全部格式、lint、strict TypeScript、构建、工作区边界和敏感数据扫描通过，high 级依赖审计仅报告 1 个不阻塞门禁的 moderate 漏洞。正式管理端原型生产构建与 4 个 Sites 包装测试通过。

final result: passed

## Ticket 20 `WEB-M05/WEB-M06` 对照目标与结论

- 应用内浏览器在精确 `1440 × 1024 CSS px` 和 `1024 × 768 CSS px` 对照正式价格计划与真实 Web → Hono → 临时 PostgreSQL 生产页。`design/source-ticket20-web-m05-*` 与 `design/implementation-ticket20-web-m05-*` 保留两侧列表、专用表单和重叠拒绝状态；对应 `design/qa-ticket20-web-m05-*-comparison-*` 把同一视口、同一状态放入同一比较输入。列表两侧均把可操作未来版本排在当前/归档版本之前，并展示门店/区域、总部机型、跨午夜时段、工作日基础/晚间/夜间及周末半小时金额、有效区间和版本状态。
- 两档视口的价格专用表单均明确显示所属范围、上海生效时间、服务端权威重叠结果和预约价格快照不变说明；两侧重叠证据都显示真实版本冲突、拒绝说明并禁用主操作。生产页以 `06:00–18:00`、`18:00–次日 00:00`、`00:00–06:00` 三段显式版本表达旧沙箱定价；相邻时段与相邻有效区间仍允许。
- 两档视口的商品列表与专用表单对照保存在 `design/source-ticket20-web-m06-*`、`design/implementation-ticket20-web-m06-*` 和对应 `design/qa-ticket20-web-m06-*-comparison-*`。总部名称、代码、分类和说明只读，本店仅开放上架、整数分售价和独立阈值，在手/预留/可用库存只读；两侧都把已引用商品的“归档配置”放在专用配置表单内，价格未来版本则在列表直接归档。
- 真实沙箱完成“服务端确认当前范围无冲突 → 创建竞技型未来价格版本 → 服务端回读未来版本 → 再次创建时由服务端返回 `v2` 重叠并禁用提交 → 归档”；迁移集成测试另行确认归档后可在同一生效时刻安全重建。商品完成“云端矿泉水下架、售价改为 `650` 分、阈值改为 `11` → 服务端重新读取成功”。两个成功反馈只在命令确认并回读后出现；新开正式源和生产标签页 warning/error 均为 0。
- 第 1 轮 P2 已关闭：生产商品表在 `1024px` 下原有 `940px` 最小宽度，保存后焦点回到末列会造成内部横向偏移并裁掉首列左侧；最小宽度收敛后容器实测 `clientWidth = scrollWidth = 858px`，页面 `bodyClientWidth = bodyScrollWidth = 1024px`，总部商品和操作列同时可见。
- 第 1 轮 P2 已关闭：正式价格表更新列结构后仍以已移除的 `name` 作为行键，控制台产生重复键告警；正式数据增加稳定版本标识并以 `id` 作为行键，新的浏览器标签复核 warning/error 为 0。
- 当前没有仍需处理的 P0、P1 或 P2 差异。正式原型使用 4 条代表性价格版本和 3 条商品行，生产展示完整 30+ 个显式价格时段版本与 12 个总部商品，因此生产表格采用受控内部纵向滚动；两侧页面职责、夜间高密度壳层、海军蓝表面、青色反馈、青柠主动作、琥珀未来/红色重叠提示、正式品牌资产和 Phosphor 图标保持一致。
- 领域与 PostgreSQL 自动化覆盖最新有效版本选择、排他结束区间与任意时段交集拒绝、跨午夜与 `06:00` 经营日逐半小时计价、旧沙箱非零兼容展示和迁移回填、预约价格快照不变、门店商品下架/改价后历史订单快照不变、归档后同生效时刻重建、跨店掩码拒绝、幂等重放和版本冲突；API 另以同一领域规则提供带角色、来源与 CSRF 校验的服务端权威重叠预检。完整 `pnpm verify` 通过：316 个 Vitest 单元测试、2 个开发启动测试、158 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 52 个 Chromium E2E；全部格式、lint、strict TypeScript、构建、工作区边界和敏感数据扫描通过，high 级依赖审计仅报告 1 个不阻塞门禁的 moderate 漏洞。正式管理端原型构建与 4 个 Sites 包装测试通过。

## Ticket 18 `WEB-S10/WEB-M09` 对照目标与结论

- 应用内浏览器在精确 `1440 × 1024 CSS px` 与 `1024 × 768 CSS px` 打开正式原型和真实 Web → Hono → 临时 PostgreSQL 生产页。`design/reference/ticket-18-web-s10-prototype-*`、`ticket-18-web-m09-prototype-*` 与 `ticket-18-web-s10-production-*` 保留两档原始截图；`ticket-18-web-s10-visual-comparison.png` 和 `ticket-18-web-m09-visual-comparison.png` 分别把选定视觉源与正式交接状态放入同一比较输入。
- `WEB-S10` 按“经营事项安全边界 → 四类快照摘要/明细 → 500 字说明 → 冻结状态 → 提交/确认双时间证据 → 同店待确认”排序。真实沙箱完成模拟签到、提交不可编辑快照、立即手动签退；DOM 复核提交后说明禁用、快照与双时间仍可读，接班确认不阻塞签退。
- `WEB-M09` 把逾期未提交、迟交和长期未确认分别列为同店只读异常，并与迟到/缺勤考勤异常并列但不混合评分；选中迟交后显示冻结的预约、订单、报修、库存摘要、补充说明、人物和双时间，不提供交接编辑或审批动作。
- 第 1 轮 P1 已关闭：生产交班已提交状态最初仍使用实时 `snapshotPreview` 渲染摘要和明细，虽然服务端冻结事实正确，后续业务变化仍可能造成页面误读；现已提交状态只渲染 `handover.snapshot`，预览仅用于提交前。
- 第 1 轮 P2 已关闭：正式店长异常页的分段计数和顶部动作最初仍沿用两项考勤异常与“追加更正”；计数已覆盖两项考勤加三项交接异常，动作明确改为“追加考勤更正”，避免暗示交接事实可修改。
- 第 2 轮 P1 已关闭：同店待确认列表最初没有返回当前人物的确认资格，未签到员工仍会看到可用确认按钮；服务端响应现逐项返回 `canConfirm`，真实页面在本人未签到时禁用动作并解释“本人已签到后可确认”，自动化先签到再确认。
- 第 2 轮 P1 已关闭：提交本人以外班次、自我确认、未签到确认以及重复提交/确认最初只返回冲突，事务回滚也丢弃拒绝证据；这些拒绝现分别以安全原因追加 `denied` 审计并保留统一对外错误边界。
- 第 2 轮 P2 已关闭：正式店员原型最初在周宁上下文提供“以赵一航确认承接”，错误表达代他人操作；原型现展示赵一航提交、周宁本人确认的独立同店交接，人物与 ADR-0010 的服务端固定上下文一致。
- 四张正式原型截图和两张生产截图均保持选定海军蓝高密度壳层、青色信息反馈、青柠唯一主动作、琥珀/红色异常层级、正式品牌资产与 Phosphor 图标。`1024px` 两页实测 `bodyScrollWidth = bodyClientWidth = 1024px`，没有页面横向溢出；两档下主操作和只读证据可通过页面内部滚动完整到达。
- 原始证据尺寸已重新读取元数据确认：`ticket-18-web-s10-prototype-1440x1024.png` 与 `ticket-18-web-m09-prototype-1440x1024.png` 均为 `1440 × 1024`，对应窄档均为 `1024 × 768`，两张合并对照均为 `1440 × 512`；正式原型与真实生产标签页 warning/error 均为 0。
- 交互与自动化覆盖冻结快照不随预约/订单/报修/库存变化、数据库禁止更新/删除、提交后立即签退、同店他人确认、三类异常物化、跨店掩码拒绝和审计，以及 API 严格正文、CSRF、幂等与总部拒绝。当前没有仍需处理的 P0、P1 或 P2 差异。
- 完整 `pnpm verify` 通过：291 个 Vitest 单元测试、2 个开发启动测试、133 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 45 个 Chromium E2E 全部通过；全部构建、格式、lint、strict TypeScript、工作区边界和敏感数据扫描通过，high 级依赖审计仅报告 1 个不阻塞门禁的 moderate 漏洞。正式管理端原型生产构建与 4 个 Sites 包装测试通过。

## Ticket 17 `WEB-S01/WEB-S09` 对照目标与结论

- 应用内浏览器在精确 `1440 × 1024 CSS px` 与 `1024 × 768 CSS px` 分别打开正式夜间运营控制台和真实 Web → Hono → 临时 PostgreSQL 生产页。`design/source-ticket17-web-s09-ready-*`、`design/implementation-ticket17-web-s09-ready-*` 与 `design/qa-ticket17-web-s09-ready-comparison-*` 保留班次页两侧和合并对照；同名 `web-s01-shift-summary` 证据覆盖工作台本人班次摘要。
- `WEB-S09` 首屏按“模拟边界 → 固定员工/门店/业务时间 → 当前班次与下一合法动作 → 不可变原始事实 → 未来/最近班次”排序。模拟边界持续声明不读取定位、人脸、门禁或真实考勤连接器；服务端只返回当前员工本人班次，不以客户端筛选代替授权。
- 正式原型固定覆盖可签到、处理中、迟到、缺勤、已签退、过早、重复提交与失败；生产真实旅程完成业务时钟下的模拟签到、原键安全重放和员工手动签退。签到后即使计划结束仍只显示手动签退，签退后没有后续动作，系统不会自动补写签退时间；跨午夜班次在班次页和工作台摘要均明确显示“次日”。
- 第 1 轮 P1 已关闭：生产 `1024px` 当前班次事实原先纵向堆叠，把青柠主动作推到首屏底边；断点改为保留三列紧凑事实后，主要动作、原始事实空态和未来班次标题重新同屏可见。
- 两档生产页面均实测 `scrollWidth = innerWidth`（分别为 `1024px` 与 `1440px`），没有页面横向溢出。自动化在 `1024 × 768` 通过键盘聚焦并按 `Enter` 提交签到，随后验证处理中禁用、手动签退、不可变事实和无自动签退；`1440 × 1024` 验证重复提交明确反馈且没有重复写入。
- 正式源和生产实现继续使用海军蓝高密度壳层、青色时间与信息反馈、青柠唯一主动作、琥珀模拟边界和右侧事实表面；时间层级、主动作、状态文本与键盘路径逐项复核后，没有仍需处理的 P0、P1 或 P2 差异。
- 完整 `JINGSHU_E2E_WEB_ORIGIN=http://127.0.0.1:3017 pnpm verify` 通过：276 个 Vitest 单元测试、2 个开发启动测试、127 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 43 个 Chromium E2E 全部通过；全部构建、格式、lint、strict TypeScript、工作区边界、敏感数据扫描和 high 级依赖审计通过，仅报告 1 个不阻塞门禁的 moderate 漏洞。正式管理端原型生产构建与 Sites 包装通过。

## Ticket 16 `WEB-S07/WEB-M02` 对照目标与结论

- 应用内浏览器在精确 `1440 × 1024 CSS px` 分别打开正式夜间运营控制台和真实 Web → Hono → 临时 PostgreSQL 生产页；待验证证据保存在 `design/source-ticket16-web-s07-verification-1440x1024.png`、`design/implementation-ticket16-web-s07-verification-1440x1024.png` 与 `design/qa-ticket16-web-s07-verification-comparison-2880x1024.png`，关闭态证据保存在对应的 `*-closed-*` 三张图片。
- 另在精确 `1024 × 768 CSS px` 复核正式 `WEB-S07` 与真实生产关闭态：页面 `scrollWidth = clientWidth = 1024px`、warning/error 为 0，领退汇总、实际消耗、解决说明、验证结果及领退双时钟均可读；Chromium 专项 E2E 在同一视口完成领用、退回、提交、失败退回、重提和成功关闭全路径。
- 真实旅程覆盖 `A-01` 报修：同店分派并开始处理，领用两副无品牌替换耳机、退回一副、提交 500 字内解决说明；切换店长后先验证失败退回处理中，再由处理人重新提交，最终独立验证成功并原子关闭报修、恢复座位正常。关闭后页面不再出现领用、退回、提交或验证动作。
- 店员详情按“合法主动作 → 已领/实际消耗 → 解决说明 → 最新验证 → 双时钟证据”排序；待验证期间座位持续维护。店长详情只在服务端允许时显示成功/失败验证，专用弹窗对失败强制要求原因、对成功允许省略备注；处理人与验证人、领用/退回业务发生时间及服务器记录时间、报修事件和审计均可追溯。
- 第 1 轮 P1 已关闭：生产关闭态最初仍先铺满八类可用备件，把实际消耗和验证结果推到首屏以下；关闭态改为优先展示领退汇总、实际消耗、解决说明与验证结果后，正式源和生产页在同一首屏表达相同终态层级。
- 两侧继续使用海军蓝高密度壳层、青色信息反馈、青柠唯一主动作、琥珀维护/失败风险与右侧证据检查器。页面实测 `scrollWidth = clientWidth = 1440px`，正式源与生产页 warning/error 均为 0；按钮焦点、失败原因保留、原幂等键重试、角色切换与关闭终态均通过，最终没有仍需处理的 P0、P1 或 P2 差异。
- 完整 `pnpm verify` 通过：261 个 Vitest 单元测试、2 个开发启动测试、113 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 41 个 Chromium E2E 全部通过；全部构建、格式、lint、strict TypeScript、工作区边界、敏感数据扫描和 high 级依赖审计通过，仅报告 1 个不阻塞门禁的 moderate 漏洞。正式管理端原型生产构建与 Sites 包装通过。

## Ticket 15 `WEB-S07` 对照目标与结论

- 视觉源：正式夜间运营控制台的 `WEB-S07` 处理中状态。正式源与生产实现继续使用海军蓝高密度壳层、青色信息反馈、青柠唯一主动作、琥珀维护风险、连续队列表面与右侧证据检查器。
- 应用内浏览器在精确 `1440 × 1024 CSS px` 打开正式源和真实 Web → Hono → 临时 PostgreSQL 生产页；`design/source-ticket15-web-s07-processing-1440x1024.png` 与 `design/implementation-ticket15-web-s07-processing-1440x1024.png` 保留两侧截图，`design/qa-ticket15-web-s07-processing-comparison-2880x1024.png` 将两者置于同一比较输入。
- 真实旅程覆盖 `A-05` 使用中预约：19:37 开始处理后座位进入维护、预约提前完成，保留 19:30–20:00 当前片段并为未来三个完整片段形成 `¥27.00` 模拟退款，不恢复体验券且不自动换座。详情同步展示公开说明、内部说明、报修事件、报修/预约审计与权威预约结果。
- 分派前座位保持正常；分派弹窗只提供同店处理人和三档优先级，开始处理弹窗明确展示维护、预约与退款影响。实际交互验证处理人与同店店长权限、状态限定、同幂等键重试以及公开/内部说明独立提交。
- 正式源使用四条合成队列与 `A-18` 影响摘要，生产页使用单条真实权威记录与 `A-05` 价格片段结果；两侧页面职责、信息层级、状态层级、风险确认和证据顺序一致。页面实测 `scrollWidth = clientWidth = 1440px`，两侧控制台 warning/error 为 0，最终没有仍需处理的 P0、P1 或 P2 差异。
- 真实 PostgreSQL 故障注入分别中断预约更新、模拟退款、预约事件、报修事件和审计写入，每次均证明报修、座位、全部预约、退款、事件、审计与命令记录整体回滚；恢复故障后复用原幂等键可成功提交。
- 同一集成组另覆盖同键并发串行化、维修与新建预约的座位锁争用、维护前过期/爽约/到时状态结算、预约终态后待支付客户订单释放和拒绝命令审计；同时证明跨店命令与不存在对象使用同一 404，总部可读审计/事件但不可读内部笔记。12 条维修事务测试与全部 106 条 PostgreSQL/API 集成测试通过。

## Ticket 14 `WEB-S06` 对照目标与结论

- 视觉源：`design/reference/selected-night-operations-console.png` 及正式店员报修创建状态。生产继续使用夜间高密度壳层、海军蓝表面、青色反馈、青柠唯一主动作、正式品牌资产和 Phosphor 图标。
- 应用内浏览器在真实 Web → Hono → 临时 PostgreSQL 路径以精确 `1440 × 1024 CSS px` 打开 `WEB-S06` 新建报修弹窗。`design/source-ticket14-web-s06-repair-modal-1440x1024.jpg` 与 `design/implementation-ticket14-staff-repair-modal-1440x1024.jpg` 保留同一页面状态、同一视口的两侧证据，`design/qa-ticket14-staff-repair-modal-comparison-2880x1024.jpg` 将两者置于同一比较输入。
- 弹窗实测座位下拉只包含棱镜旗舰店座位，选择 `A-18` 后区域与竞技型自动派生；500 字计数、个人信息提示、图片预览/删除、权限拒绝说明、内置样例和单图代理提交均可操作。
- 新建后队列立即出现权威报修行且 `A-18` 仍为正常运营状态；再次选择同一座位提交会打开现有报修，不增加重复行。图片失败与净化失败使用独立错误反馈，不阻塞文字记录。
- 正式源的创建态只表达优先级与说明，生产按已批准契约扩展为座位、自动机型、说明和私有图片恢复区；两侧的壳层密度、分隔线、圆角、标题层级、弹窗焦点、遮罩层和底部动作区一致。生产创建态的必要扩展没有改变 `WEB-S06` 的信息层级，控制台 warning/error 为 0。最终没有仍需处理的 P0、P1 或 P2 差异。

## Ticket 13 `WEB-S08/WEB-M06` 对照目标与规范化

- 视觉源：`design/reference/selected-night-operations-console.png` 及其正式店长库存页。两侧继续使用深色高密度壳层、海军蓝表面、青色数据反馈、青柠主操作、琥珀低库存、正式 PNG 品牌标志和 Phosphor 图标。
- 同视口比较：应用内浏览器在 `1440 × 1024` 与 `1024 × 768 CSS px` 分别打开正式库存页和真实 Web → Hono → 临时 PostgreSQL 生产页；两侧统一为许知远店长、棱镜旗舰店、20 个库存项目、12 个商品、八类备件、3 项低库存和三条最近流水。
- 证据：`design/comparison-ticket13-inventory-1440x1024.png` 与 `design/comparison-ticket13-inventory-1024x768.png` 将正式源和生产实现放入同一比较输入；同名 `source-*`、`implementation-*` 保留两侧原始截图，`implementation-ticket13-stocktake-dialog-1440x1024.png` 保留盘点弹窗证据。
- 信息架构：标题与动作、四格库存摘要、搜索/类别/告警筛选、八列表格和最近流水保持正式源顺序；生产表格在固定高度内滚动全部 20 项，使摘要、余额和流水在桌面首屏保持共同可见。
- 状态规范化：生产通过普通商品的手工入库、盘点与关联补偿构造三条真实流水，不改变 3 项低库存；正式原型使用等量合成流水。两侧只规范页面职责、密度、状态表达和操作可达性，库存名称、时钟与流水原因继续来自各自权威状态。

## Ticket 13 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：生产库存表最初完整撑开 20 行，使最近流水离开 `1440 × 1024` 首屏；表格改为内部滚动后，六行余额、最近流水标题与三条流水重新和正式源同屏，20 项仍可滚动访问。
- 第 1 轮 P2 已关闭：`1024 × 768` 初始断点把标题动作和筛选强制折为两行，最近流水被固定页脚遮挡；断点收窄并为该视口设置表格高度后，动作、摘要、筛选、余额与最近流水层级对齐，`bodyScrollWidth = documentWidth = 1024px`。
- 三联库存数字逐行满足 `可用 = 账面 − 预留`；告警同时使用文字、琥珀色和摘要计数，不只依赖颜色。最近关联流水与下方账本使用同一动作名称、差额、时间、原因和原流水关联结果。
- 盘点、入库、补偿均为专用 `dialog`，没有通用命令字段。盘点在提交前实时显示“账面 → 实际；差额”；实测弹窗 `560 × 542 CSS px`，支持首字段聚焦、Tab 循环、Escape 关闭和提交后焦点返回。

## Ticket 13 状态与交互验证

- 店员库存页只显示余额和“余额只读”，不存在手工入库、盘点或补偿按钮；店长页提供三个专用动作。顾客与总部角色不能读取或直接改量，跨店库存项目也被拒绝。
- 手工入库只接受正整数和安全原因；成功后账面、可用、告警、流水与审计在同一事务更新。盘点以实际数量计算差额并追加流水，不覆盖历史余额事件。
- 补偿接受非零整数差额，可关联同店同项目原流水；实测“手工入库 +2”后追加“补偿 -1”，新流水显示“已关联原流水”，原流水内容保持不变。
- 在真实沙箱完成低库存项目盘点与入库后，告警从触发转为解除；随后在普通商品构造三条流水保持 3 项初始告警，用于与正式源同状态视觉比较。最近流水展开/收起、搜索和筛选均可操作。
- `1440 × 1024` 与 `1024 × 768` 页面均无整体横向溢出；宽表只在自身容器内部滚动。正式和生产标签页的浏览器 warning/error 均为 0。

## Ticket 13 数据与自动化验证

- 真实 PostgreSQL 集成覆盖 12 商品/八类备件、三联恒等式、阈值边界、每项最新流水不受全店 20 条窗口影响、入库幂等、盘点差额、补偿关联和原流水不变；非法原因/数量、无变化、预留冲突、原流水不存在、幂等冲突、不存在项目与跨店请求均证明库存和命令整体回滚且追加拒绝审计。迁移回归另证明旧版本写入方可继续省略新增流水类型列。
- API 集成覆盖店员/店长同店读取、顾客/总部/店员写入拒绝、Origin/CSRF/幂等键、严格专用正文、原因与整数约束，以及成功结果映射；跨店和不存在项目对外使用相同 404 代码与文案，不泄露对象存在性。
- Chromium E2E 覆盖店员只读、角色切换、三种店长动作、动态差额、最近流水展开/收起、`560 × 542` 弹窗和精确 `1024 × 768` 页面宽度；正式原型生产构建通过。
- 完整 `pnpm verify` 通过：261 个 Vitest 单元测试、2 个开发启动测试、86 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 40 个 Chromium E2E 全部通过；全部构建、格式、lint、strict TypeScript、工作区边界、敏感数据扫描和 high 级依赖审计通过，仅报告 1 个不阻塞门禁的 moderate 漏洞。

## Ticket 12 `WEB-S04/S05` 对照目标与规范化

- 视觉源：`design/reference/selected-night-operations-console.png`。正式订单页和生产订单页沿用同一深色高密度壳层、海军蓝表面、青色数据反馈、青柠唯一主动作、琥珀处理中/风险和红色取消错误，并继续使用正式 PNG 品牌标志与 Phosphor 图标。
- 同视口比较：应用内浏览器在 `1440 × 1024` 打开选定视觉源对应的正式店员壳层与商品订单页，把参考图和实现截图放入同一比较输入；另用生产自动化在精确 `1024 × 768` 验证队列、详情、取消弹窗和页面宽度。正式与生产使用相同角色、门店和夜间令牌；真实沙箱无订单时保留权威空态，数据密度用正式原型的合成订单复核，不在生产中伪造记录。
- 信息架构：主区按阶段 Tab、搜索、订单队列组织；行内保持顾客/座位、商品摘要、模拟金额、状态与等待时间；右侧检查器按身份与下一动作、商品快照、库存/关联、不可变事件排序。该层级与选定工作台源的“当前任务—摘要—详情”结构一致。
- 状态规范化：正式原型用 `orderCommandState=processing|failure` 固定处理中和失败状态；生产测试通过受控 API 响应观察禁用主动作、原状态保留、原幂等键重试和成功结果。两者都禁止任意状态选择与跨级推进。

## Ticket 12 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：正式原型取消弹窗最初把焦点放在关闭按钮，无法直接填写必填原因；共享 `Modal` 增加显式初始焦点目标后，订单取消弹窗打开即聚焦原因文本框，仍保留 Tab 循环、Escape 关闭与焦点返回。
- 第 1 轮 P2 已关闭：生产订单表的桌面最小宽度比可用主区多约 `4px`，空队列也显示无意义的内部横向滚动条；最小宽度收敛后应用内浏览器复核 `mainScrollWidth = mainClientWidth = 716px`，表格保持 `688px`，warning/error 为 0。精确 `1024px` 下横向滚动只留在确实超宽的队列表面，`bodyScrollWidth = bodyClientWidth = 1024px`。
- 第 2 轮 P1 已关闭：主要动作失败后曾为重试生成新幂等键，切换订单后的短暂读取窗口也曾保留上一单详情；重试上下文现同时绑定订单、动作与原因，失败后复用原键，切单则同步清空详情和重试上下文，服务端新详情返回前不提供旧动作。
- 第 2 轮 P2 已关闭：店员时间线曾使用与服务端不一致的制作/待取事件名，顾客取消终态也只回退到支付阶段；事件标签现直接覆盖 `order.preparing` 与 `order.ready-for-pickup`，顾客终态进度按不可变时间线的最高历史阶段展示。
- 交互复核：阶段筛选真实缩小队列；详情行可键盘打开；开始制作、标记待取、完成严格逐级；取消原因空值禁用确认；失败后弹窗保留且展示“原状态保持不变”，重试成功明确显示全额模拟退款与库存损耗/释放。完成结果显示库存销售和成长值。

## Ticket 12 必查保真表面

- 字体与排版：Noto Sans SC Variable、紧凑标题/说明/表头层级、等宽订单号和表格数字保持一致；`1440 × 1024` 队列与详情同屏可读。
- 间距与布局：侧栏、主区和 `360px`/响应式检查器沿用共享壳层轨道；`1024 × 768` 页面无整体横向溢出，详情不遮住主要操作。
- 颜色与资产：深色表面、青色状态、青柠主按钮、琥珀等待/失败、红色取消反馈均来自既有令牌；没有占位图、手写 SVG、文本符号或 CSS 伪造图标。
- 文案与内容：模拟支付/退款不扣款、关联预约独立终止、整单库存预留、制作后损耗、体验券恢复边界和成长值均使用领域契约术语。
- 状态与交互：筛选、打开详情、开始制作、标记待取、完成和取消均有读取/处理中、成功与失败可观察反馈；唯一合法主动作由服务端当前状态决定。
- 响应式与无障碍：表格在窄主区使用受控内部滚动，页面本身不横向溢出；取消对话框具有语义化 `dialog`/`alert`、原因初始焦点、焦点循环、Escape 和焦点返回，风险不只靠颜色表达。

## Ticket 12 数据与自动化验证

- 领域测试穷举七种订单状态与开始制作、标记待取、完成、取消四种动作，覆盖制作前释放/恢复券和制作后损耗/不恢复券。
- 真实 PostgreSQL/API 集成覆盖门店与角色隔离、预约终止后继续履约、跨级/重复拒绝及审计、完成销售流水/模拟营业额/成长值、制作前后全额模拟退款，以及顾客制作前取消。
- 完成事务在写入库存销售流水后注入成长事件故障，API 返回可安全重试的整体失败；订单仍为待取、库存余额和预留不变，流水、成长、完成事件、允许审计与幂等命令记录均为零，证明同一事务整体回滚。
- Chromium E2E 覆盖队列筛选、详情、逐级动作、取消弹窗、处理中、失败、原键重试、切单旧动作隔离、规范事件文案、成功结果和精确 `1024 × 768` 页面宽度。
- 完整 `pnpm verify` 通过：256 个 Vitest 单元测试、2 个开发启动测试、76 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 39 个 Chromium E2E 全部通过；全部构建、格式、lint、strict TypeScript、工作区边界、敏感数据扫描和 high 级依赖审计通过，仅报告 1 个不阻塞门禁的 moderate 漏洞。

## Ticket 11 `WEB-C04` 对照目标与规范化

- 信息架构与移动视觉源：正式小程序 `MP-10` 柜台商品、`MP-11` 商品确认及 `MP-12` 商品订单详情；生产实现沿用 Web 深色令牌、青色数据反馈、荧光绿主操作、琥珀缺货提示和 Phosphor 图标，并保留共享 Web 沙箱工具栏。
- 同视口比较：应用内浏览器把正式参考与真实 Web → Hono → 临时 PostgreSQL 路径统一为 `360 × 800 CSS px`。`design/ticket11-comparison-mp10-empty-cart.jpg`、`design/ticket11-comparison-mp10-low-stock.jpg`、`design/ticket11-comparison-mp11-confirm.jpg`、`design/ticket11-comparison-shortage.jpg`、`design/ticket11-comparison-pending.jpg` 与 `design/ticket11-comparison-mp12-success.jpg` 分别将正式源和生产截图放入同一比较输入；同名 `source-*` 与 `ticket11-web-c04-*` 保留两侧原始证据。
- 状态规范化：正式源使用设备框和小程序栏展示同一故事；生产使用 Web 共享壳、已到店预约 `A-07`、真实门店目录与服务器库存。正式 `MP-11` 的一次确认动作在生产中按领域契约拆为“创建待模拟支付订单”和独立“不扣款”模拟支付，以保留十分钟整单库存和体验券占用。
- 跨端边界：两侧比较页面职责、信息层级、状态、交互和夜间视觉语言，不要求设备外壳、系统状态栏或共享 Web 工具栏像素等值。金额、券、商品行和主操作的层级保持一致。

## Ticket 11 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：真实沙箱中模拟支付请求最初没有发送 API 契约要求的空 JSON 对象，页面正确停留在待支付并显示安全错误；实现补齐 `Content-Type` 与 `{}` 正文后，原提交标识重试成功且没有重复库存副作用。
- 第 1 轮 P2 已关闭：整单不足截图初始停留在确认页顶部，错误卡在首屏下方；最终证据滚动到错误卡、原子回滚说明和固定提交栏同屏，清楚证明购物车、金额与恢复动作未丢失。
- 第 2 轮 P2 已关闭：确认页取消商品体验券后，候选券最初随选中状态一起消失，无法在当前页面重新选择；现改为持续展示全部候选券，并支持“未使用 / 未达门槛 / 已使用”反复切换，`WEB-C04` E2E 覆盖取消与重新使用。
- 可接受差异：生产目录增加返回预约详情、Web 沙箱标题和门店/座位关联卡；正式参考保留小程序系统栏与底栏。生产待支付详情把库存保留倒计时和不可变事件前置，成功结果单独强调全程没有扣款，再允许返回订单详情。

## Ticket 11 状态与交互验证

- 空购物车：六种本店商品、分类 Tab、门店价、可用库存和低库存阈值均来自服务端；`0 件商品`、`¥0.00` 与禁用提交栏一致。
- 低库存与确认：数量步进为整数且不超过可用库存；低库存 `3` 清晰分级。购物车件数、小计、体验券减免和应付模拟金额在内容区与固定栏完全一致。
- 整单不足：在真实 PostgreSQL 临时沙箱把一行可用库存降至零后提交，服务端整单回滚；页面显示安全缺货消息、保留商品行与金额，恢复库存后使用原确认内容重试成功。
- 待支付与成功：订单详情显示 `10:00` 起始倒计时、不可变商品/金额快照、五步进度和事件；模拟支付入口与结果均明确不扣款。成功页显示服务端确认金额，并说明预约后续终止不会取消已支付订单。
- 应用内浏览器完成已到店预约、目录、缺货、重试和模拟支付的完整真实交互；`360 × 800` 页面无横向溢出，自动化检查所有主控件不小于 `44px`。上述六组比较输入复核后 P0/P1/P2 清零。

## Ticket 11 数据与自动化验证

- 领域测试覆盖订单金额、体验券封顶、支付前终态取消及支付后预约独立性；真实 PostgreSQL 集成覆盖目录隔离、快照、幂等创建/支付、整单回滚、超时/取消释放和二十并发请求的库存上限。
- API 集成覆盖顾客角色、Origin/CSRF/幂等键与严格正文边界、跨顾客对象拒绝和安全中文错误；`WEB-C04` Chromium E2E 在 `360 × 800` 覆盖空购物车、低库存、体验券取消/重选、缺货重试、倒计时、成功与横向溢出。

## Ticket 10 `WEB-C03` 对照目标与规范化

- 信息架构与移动视觉源：正式小程序 `MP-16` 统一行程与 `MP-17` 会员中心；生产实现复用 Web 深色令牌、青色数据反馈、青柠当前状态、Phosphor 图标和共享 Web 沙箱工具栏。
- 同视口比较：应用内浏览器把正式参考和真实 Web → Hono → 临时 PostgreSQL 生产路径统一为 `360 × 800 CSS px`。`design/comparison-ticket10-journey-states-360x800.png` 同屏覆盖当前行程和历史空筛选；`design/comparison-ticket10-membership-states-360x800.png` 同屏覆盖白银档案以及体验券可用、占用中、已使用、已过期四态，同名 `source-*`/`implementation-*` 保留原始截图。
- 状态规范化：正式参考固定白银 `860`、黄金门槛 `1500` 和四态券；生产使用同一会员档案，并通过真实预约生命周期让预约券从可用进入占用、到期释放或完成后已使用。Web 历史空态通过“含模拟退款”真实筛选到达，不伪造第二份顾客档案或删除权威历史。
- 跨端边界：正式参考保留设备框、本地演示条和小程序底栏；生产保留共享 Web 工具栏、Web 独立沙箱标识和 Web 底栏。两侧只比较页面职责、信息层级、状态、交互和视觉语言，不要求设备外壳像素等值。

## Ticket 10 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：顾客 H5 的成员表面最初没有真实可达的历史空态；新增“全部历史/含模拟退款”筛选后，空筛选显示原因和“查看全部历史”恢复入口，真实无历史数据仍显示预约入口。
- 第 1 轮 P2 已关闭：服务端终态原因代码曾直接显示为 `planned-end-auto-completed`；顾客表面现映射为“计划结束后自动完成”，保留员工输入的合成中文原因作为安全回退。
- 正式 `MP-16` 修正生命周期分组：当前只显示待确认、已确认、已到店、使用中等非终态对象；完成、取消、过期预约、结束订单和已关闭报修进入历史，当前/历史空态均有明确恢复动作。
- 可接受差异：生产会员页增加 Web 沙箱边界说明并以较高信息密度展示券金额、规则和关联状态；正式参考用设备框与小程序内容密度展示相同层级，核心字段、四态与主操作一致。

## Ticket 10 状态与交互验证

- 一级导航：预约、门店、行程、会员均为可操作根导航；切换根页会回到滚动顶部，固定头部和底部安全区在 `360px` 下不遮挡内容。
- 统一行程：当前、未来、历史三组可切换；当前/历史预约卡、模拟退款与成长值关联均可回到权威预约详情，详情可返回统一行程。历史筛选空态可恢复到全部历史。
- 会员与体验券：白银 `860`、还差 `640` 和黄金 `1500` 同时可见；可用、占用中、已使用、已过期四个 Tab 均有服务端内容，占用/已使用券可打开关联预约，成长记录可打开同一历史预约。
- 演示边界：页面持续显示“演示数据”和 Web 独立沙箱说明；整页无“积分”“余额”“返现”，并明确不提供储值、转让、折现或手工调整。
- 应用内浏览器完成上述路径和同屏视觉复核，正式参考与生产控制台 warning/error 均为 0；两组比较输入复核后 P0/P1/P2 清零。

## Ticket 10 数据与自动化验证

- 领域测试覆盖青铜/白银/黄金边界、成长值取整、终身不降级、完成幂等发放，以及取消/过期/设备退款不重复发放和体验券四态。
- 真实 PostgreSQL/API 集成覆盖主演示白银 `860`、完成预约净额成长、重复完成不重复发放、待确认券占用、到期释放、顾客/角色/沙箱隔离及拒绝查询字符串枚举。
- `tests/e2e/customer-seat-browse.spec.ts` 的两条 `WEB-C03` 用例在 `360 × 800` 覆盖四态券、关联券/成长跳转、行程 Tab、历史筛选空态、触控目标和横向溢出；正式小程序运行时完整性与生产构建通过。

## Ticket 09 `WEB-S01/S02/S03` 对照目标与规范化

- 视觉源：正式管理端原型的店员工作台与预约列表，继续使用选定的“夜间运营控制台”方向、深色高密度布局、荧光绿主操作、青色反馈、琥珀/红色异常分级和 Phosphor 图标。
- 同视口比较：应用内浏览器把正式源与真实 Web → Hono → 临时 PostgreSQL 生产路径统一为 `1440 × 1024` 和 `1024 × 768 CSS px`。源图、实现图和同屏比较输入均保存在 `design/`。
- 状态规范化：正式源与生产实现使用同一店员人物、棱镜旗舰店范围与固定业务时钟；生产队列按本票契约从旧原型示例的通用分组规范化为“到店窗口、已到店待开始、使用中、异常预约”，预约列表补齐状态、时间、区域、机型和异常五类服务端筛选。
- 证据：`comparison-ticket09-workbench-1440.png`、`comparison-ticket09-reservations-1440.png`、`comparison-ticket09-workbench-1024.png` 与 `comparison-ticket09-reservations-1024.png`；同名 `source-*` 和 `implementation-*` 文件保留两侧原始截图。

## Ticket 09 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：已到店但尚未到计划开始时间的预约没有合法主要动作，检查器最初沿用终态文案。修复后改为“已完成到店登记；计划开始后才可开始使用”，终态继续单独说明状态已结束且不显示冗余按钮。
- 工作台队列、数量下钻、列表五类筛选、计划时间、顾客工作名与到店窗口保持正式原型的信息密度；右侧检查器继续使用相同层级呈现状态、唯一主要动作、价格快照、关联业务和不可变事件。
- `1440px` 桌面态保留完整导航、主工作区与检查器；`1024px` 管理适配收起导航但不裁切筛选、队列或详情。两种视口均无页面横向溢出。

## Ticket 09 状态与交互验证

- 到店：真实数据库验证开始前 30 分钟含边界、爽约边界前最后一毫秒仍可办理；到开始后 15 分钟的精确边界会先按既有爽约规则过期。确认后服务端状态、到店事件和工作台队列同步更新，窗口外没有非法主操作。
- 开始使用：只有已到店且达到计划开始时间的预约显示动作；尚未开始时显示明确等待说明，不能通过任意状态选择器绕过。
- 提前结束：使用中预约必须输入不超过 200 字、纯文本且不含敏感信息的原因；成功后进入完成终态、记录原因事件并移除主操作。到计划结束仍在使用中的预约由业务时钟幂等自动完成。
- 取消：店员或店长只能在使用前取消；原因弹窗包含隐私提示，输入有效前确认按钮禁用。已确认预约取消后原子生成快照金额退款、恢复体验券并进入无主操作终态。
- 焦点与导航：命令弹窗支持焦点循环、`Escape` 关闭和焦点返回；`1024px` 侧栏收起后工作台、预约列表与检查器仍可导航和操作。
- 跨角色：同一沙箱中由店员办理到店、使用、完成或取消后，切回顾客 H5 会从服务端读取相同权威状态，不显示客户端乐观结果。

## Ticket 09 数据与自动化验证

- 领域状态机穷举每个动作与预约状态的允许/拒绝组合，并覆盖到店窗口前后边界、计划开始边界和自动完成决策。
- 真实 PostgreSQL 覆盖当前经营日队列/筛选、命令幂等、事务回滚、预约事件、模拟退款、体验券恢复、业务时钟自动完成、非法状态和跨店拒绝审计。
- API 集成覆盖店员/店长鉴权、Origin/CSRF、门店范围、原因约束、详情和命令结果，以及顾客 → 店员 → 顾客的权威状态回读。
- `tests/e2e/role-context-shell.spec.ts` 覆盖队列下钻、五类筛选、详情唯一主要动作、原因弹窗、终态无冗余按钮和 `1024 × 768` 布局；正式原型构建与 Sites 包装测试通过。
- 应用内浏览器在真实生产路径完成到店、取消/退款和提前结束，正式源与生产实现的四个同屏输入逐项复核后 P0/P1/P2 清零；控制台只有 React 开发提示和热更新信息，无 warning/error。

## Ticket 08 `WEB-C02` 对照目标与规范化

- 信息架构与移动视觉源：正式小程序原型 `MP-08` 模拟支付和 `MP-09` 预约详情；生产实现继续复用管理端深色令牌、青色数据反馈、青柠主操作和 Phosphor 图标。
- 同视口比较：应用内浏览器把正式源和真实 Web → Hono → 临时 PostgreSQL 的生产路径统一为 `360 × 800 CSS px`。源图、实现图与 `720 × 800` 并排比较输入均保存在 `design/`。
- 状态规范化：两小时预约、旗舰店竞技型、区域 A、预约立减体验券与 `¥21.00` 快照；日期、座位编号和到店窗口来自各自固定演示状态，只比较信息层级、组件结构、状态表达和操作可达性。
- 证据：`qa-ticket08-payment-confirm-comparison-720x800.png`、`qa-ticket08-detail-pending-comparison-720x800.png`、`qa-ticket08-detail-confirmed-comparison-720x800.png`、`qa-ticket08-detail-cancelled-comparison-720x800.png` 与 `qa-ticket08-detail-expired-comparison-720x800.png`。

## Ticket 08 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：生产预约详情最初只有状态胶囊，没有正式 `MP-09` 的三阶段生命周期进度。修复后加入“已确认—已到店—使用中”进度轨，当前节点、已完成节点和终态均使用文字、编号或勾选共同表达。
- 模拟支付确认页的盾牌主视觉、金额层级、安全说明、青柠主操作和次操作与 `MP-08` 保持一致；生产保留真实 Web 壳层，不复制原生设备框。
- 详情页的到店窗口、状态、进度轨、预约快照、价格/体验券、模拟退款、关联业务空态和不可变时间线与 `MP-09` 保持相同信息优先级。待确认、已确认、已取消与已过期四种状态在 `360px` 无横向溢出，主操作不小于 `44px`。

## Ticket 08 状态与交互验证

- 模拟支付：确认、处理中、成功、失败都明确说明不扣款和不需要真实支付凭证；失败/超时保留原提交标识重试，成功进入已确认并展示原金额快照。
- 生命周期：真实固定时钟验证十分钟待确认倒计时、已确认、精确开始后十五分钟爽约过期，以及再次读取不重复事件/退款；七种状态均由自动化覆盖。
- 顾客取消：待确认取消不退款；已确认且未开始的预约输入原因后生成 `¥21.00` 全额模拟退款并恢复体验券。取消弹窗、取消后终态和退款说明均在应用内浏览器实测。
- 终态：已取消和已过期只提供再次预约与刷新，不显示非法支付或取消动作；详情持续显示不可变时间线和关联业务空态。

## Ticket 08 数据与自动化验证

- 领域固定时钟测试覆盖合法/非法状态转换、十分钟保留边界、开始后十五分钟爽约边界和副作用决策。
- 真实 PostgreSQL 覆盖支付幂等重放、事务回滚后同键重试、取消退款/恢复体验券、待确认取消、精确到期、爽约、时间推进、非法转换当前状态及过滤审计。
- API 集成覆盖鉴权、Origin/CSRF、请求体、幂等、支付/取消/详情成功与可行动错误；`tests/e2e/customer-seat-browse.spec.ts` 覆盖模拟支付成功、取消退款、失败原键重试、七种详情状态和终态非法动作缺失。
- 应用内浏览器使用真实生产路径完成待确认 → 模拟支付 → 已确认 → 取消/退款，以及业务时间原子推进到爽约过期；四个关键详情状态与正式源在同一次视觉输入复核后 P0/P1/P2 清零。

## Ticket 07 `WEB-C01` 对照目标与规范化

- 信息架构与移动视觉源：正式小程序原型 `MP-07`（`product-ui/miniprogram/design-prototype/?screen=MP-07`）；实现复用管理端深色令牌、青色数据反馈、青柠主操作和 Phosphor 图标。
- 同视口比较：应用内浏览器把正式 `MP-07` 与真实 Web → Hono → 临时 PostgreSQL 的 `WEB-C01` 确认状态统一为 `360 × 800 CSS px`，并在同一次 `720 × 800` 并排比较输入中复核。
- 状态规范化：两小时立即预约、旗舰店竞技型、区域 A、明确可订座位、预约立减体验券；源图的示例日期、座位与金额只作版式参考，生产值来自沙箱上海业务时钟、实时座位查询和服务端整数分计价。
- 受控差异：正式原型主操作继续到“模拟支付”；ticket 07 的生产主操作按当前业务边界改为“创建十分钟保留”，支付、取消与到期转换留给 ticket 08。

## Ticket 07 Findings

- 最终没有仍需处理的 P0、P1 或 P2 差异。
- 第 1 轮 P1 已关闭：确认标题最初把区域后缀与完整座位编号重复成“竞技区 A A-05”。修复后标题与成功快照统一为“竞技区 A-05”，对齐正式 `MP-07` 的“竞技区 A-18”结构。
- 预约快照、三步进度、信息卡、金额层级、价格展开、体验券卡、不扣款说明和固定主操作与正式源保持相同信息优先级；真实壳层品牌栏、服务端业务时间和合成数据徽标继续保留。
- `360px` 确认页与成功页无横向溢出，可操作按钮均不小于 `44px`；固定主操作为末尾内容预留空间，错误状态同时用图标、边框与文字表达。

## Ticket 07 状态与交互验证

- 无券：显示“不使用体验券”和空券解释，按价格片段原价继续。
- 可用券：默认选择预约立减券，减免与应付金额同步更新；取消选择后恢复原价。
- 不可用券：最低金额、门店、业务、时段、有效期或已占用原因使用文字解释，不只置灰。
- 零应付：折扣封顶于小计，明确展示 `¥0.00`，不会出现负数。
- 冲突失效：服务端座位/顾客冲突使当前选择失效并返回选座；体验券冲突清除券并可重新确认；失败不显示业务成功。
- 成功保留：真实应用内浏览器完成价格展开、取消/重选体验券和创建命令，进入“预约已排他保留十分钟”，展示截止时间、快照和“没有发生扣款”。

## Ticket 07 数据与自动化验证

- 领域测试覆盖券的业务类型、门店、时段、最低金额、有效期、占用状态与零应付封顶。
- 真实 PostgreSQL 使用座位和顾客两个 `tstzrange` 排斥约束；20 个同座同窗并发请求仅一个成功，同一顾客跨店重叠并发仅保留一个合法结果。
- 强制业务事件写入失败时，预约、体验券占用、业务事件、审计和命令键计数均为零；移除故障后同一命令可以安全重试。
- API 集成覆盖 CSRF/Origin、同键同负载重放、同键不同负载稳定冲突和完整快照；`tests/e2e/customer-seat-browse.spec.ts` 覆盖 `360px` 五类设计状态及成功提交。
- 应用内浏览器实测 `document.scrollWidth === clientWidth`、H5 内部无横向溢出、可操作按钮无小于 `44px` 项；正式源与生产实现在同一次视觉输入复核后 P0/P1/P2 清零。

## Ticket 06 顾客 H5 对照目标与规范化

- 信息架构源：小程序 `../..` 对应的 `product-ui/miniprogram/design-prototype/design/reference/selected-reservation-first-home-393x852.png`（`393 × 852`）与 `product-ui/miniprogram/design-prototype/design/interaction-seat-map-final.png`（`393 × 852`）。
- Web 视觉语言源：`design/reference/selected-night-operations-console.png`；顾客 H5 沿用炭灰/海军蓝表面、青色数据反馈、青柠主操作和 Phosphor 图标，不复制管理台的桌面密度。
- 同视口实现：`design/implementation-ticket06-customer-conditions-393x852.jpg`、`design/implementation-ticket06-customer-seats-393x852.jpg`；两张截图均来自真实 Web → Hono → 临时 PostgreSQL 路径，CSS 视口与截图均为 `393 × 852`。
- 360px 证据：`design/implementation-ticket06-customer-conditions-360x800.jpg`、`design/implementation-ticket06-customer-seats-360x800.jpg` 与 `design/implementation-ticket06-customer-seats-selected-360x800.jpg`；CSS 视口与截图均为 `360 × 800`。
- 同一比较输入：`design/qa-ticket06-conditions-comparison.png` 与 `design/qa-ticket06-seats-comparison.png` 均为 `786 × 852`，左侧是正式信息架构源，右侧是同状态生产实现。
- 状态对齐：两小时立即预约、旗舰店竞技型、当前半小时片段起点、区域 A、未选座位和选中 `A-05`；静态源图中的示例业务时间与金额仅作版式参考，生产结果以沙箱上海业务时钟、半小时规则和整数分服务端计算为准。

## Ticket 06 Findings

- 没有仍需处理的 P0、P1 或 P2 差异。
- 信息架构按票据受控扩展：正式小程序首页把已选门店压缩为单行，生产 `WEB-C00` 在预约条件前展开三张门店卡，明确展示三店营业时间、96/64/40 座、区域和 Web 独立沙箱边界；固定“查找可订座位”仍持续可见，不因扩展内容失去主任务。
- 第 1 轮 P1 已关闭：底部导航、查找座位和座位确认最初仅在滚动接近页面末尾时吸附，未达到源图的固定操作区。修复后 `360px` 与 `393px` 均固定在安全区上方；列表末尾增加等量留白，不遮挡价格片段或最后一行座位。
- 第 1 轮 P2 已关闭：三步进度的不可换行最小内容曾把卡片撑到 `488px`，第三步在窄视口被裁切。修复后所有滚动内容子项和进度网格允许收缩，三步居中分布；自动化同时检查 H5 内部 `scrollWidth <= clientWidth`。
- 座位图只显示当前区域与机型的 16 个匹配座位，以四列、`72px` 高触控卡实现等价局部导航；可订、已预留、使用中和维护中同时使用图标、边框样式及文字，不以颜色作为唯一编码。
- 选择 `A-05` 后座位卡、底部摘要和“完成预览”同步更新；返回并切到竞技区 B 会自动重查并显示“选择已清除”原因。预览边界明确说明 ticket 06 不创建预约，写入流程留给 ticket 07。

## Ticket 06 必查保真表面

- 字体与排版：Noto Sans SC Variable；标题、眉题、数字、说明和固定操作保持正式层级，`360px` 无逐字换行或裁切。
- 间距与布局：预约条件和座位图均为单列移动布局；门店卡、分段控件、区域横向局部导航、四列座位和固定操作区在 `360px` 保持可读，页面及 H5 内部无横向溢出。
- 颜色与资产：复用管理端深色令牌与正式品牌 PNG，主操作使用青柠、数据与当前步骤使用青色、维护使用暖色；所有功能图标来自 Phosphor，无手写 SVG、文本符号或占位资产。
- 文案与内容：三家正式虚构门店、合成数据边界、上海业务时间、未来七天、半小时对齐、1–8 小时、机型体验描述、价格片段依据和模拟支付不扣款均明确可见。
- 状态与交互：三店选择、立即/未来切换、时间输入、时长步进、区域/机型筛选、自动重查、失效选择清除、座位选择、返回修改、错误重试和底栏入口均可操作。
- 触控与安全区：生产按钮不小于 `44 × 44 CSS px`；底部导航和固定主操作叠加 `env(safe-area-inset-bottom)`，最后一行内容不被覆盖；风险和不可用状态都有文字标签。

## Ticket 06 浏览器、API 与数据库验证

- Codex 应用内浏览器：真实 `393 × 852` 和 `360 × 800` 顾客路径完成三店浏览、区域/机型筛选、座位图、`A-05` 选择及区域变更清除选择；全新标签 warning/error 为 0。
- 性能与响应式自动化：`tests/e2e/customer-seat-browse.spec.ts` 3/3 通过；服务端可订性响应结束后首个可操作状态小于 1 秒，本地座位选择到下一帧小于 100ms，`360px` 页面和 H5 内部均无横向溢出，所有可操作按钮不小于 `44px`。
- 真实 PostgreSQL/API：迁移与升级测试通过；三店目录、区域/机型计数、运营状态、重叠预约推导、可解释价格和非顾客拒绝集成测试通过。领域测试覆盖 06:00 经营日、当前半小时、普通/跨午夜营业边界、未来七天、周末系数与整数分片段。

## Ticket 06 对比历史

### 第 1 轮

- P1：固定操作区只在接近内容末尾时出现；P2：三步进度的最小内容宽度导致窄屏内部裁切。
- 修复：移动断点把查找/确认操作和底栏固定到安全区，内容补足底部留白；滚动内容子项与三步网格统一 `min-width: 0` 并重新居中。
- 修复后证据：两张最终同屏比较图、三张 `360 × 800` 实现图，以及 H5 内部宽度和触控尺寸自动化断言。

### 最终复核

- 源图和生产截图已放入同一次比较输入，逐项复核字体、间距、颜色、资产、文案、座位图例、固定操作区和安全区；P0/P1/P2 清零。

## 对照目标与规范化

- 视觉源真相：`design/reference/selected-night-operations-console.png`（`1487 × 1058`），以及在同一视觉系统内维护的正式状态源图。
- 时间确认源图/实现：`design/source-ticket05-time-confirm-1440-final.png` 与 `design/implementation-ticket05-time-confirm-1440-final.png`。
- 重置影响源图/实现：`design/source-ticket05-reset-impact-1440-final.png` 与 `design/implementation-ticket05-reset-impact-1440-final.png`。
- 视口与密度：全视图均为 `1440 × 1024 CSS px`、`devicePixelRatio = 1`，截图像素均为 `1440 × 1024`，无需密度换算；角色均为店员，最终时间确认对照使用相同上海业务时间。
- 响应式实现证据：`design/implementation-ticket05-time-preview-1024.png`，`1024 × 768 CSS px`、`devicePixelRatio = 1`。
- 状态：两种推进方式、影响确认、原子处理中、成功、事务失败、24 小时上限、重置影响、二次确认、创建成功、创建失败与旧标签失效。
- 本轮响应式源：正式 `1024px` 状态源 `design/source-ticket04-role-shell-1024.png`，以及用户在应用内浏览器提交的 4 张标注截图；后者分别指出 `1274px` 顶栏图文未对齐、`883px` 主任务裁切、`671px` 摘要文字竖排和 `619px` 等待回执裁切。
- 本轮实现证据：`design/implementation-browser-comments-responsive-1440.png`、`design/implementation-browser-comments-responsive-1274.png`、`design/implementation-browser-comments-responsive-1024.png`、`design/implementation-browser-comments-responsive-883.png`、`design/implementation-browser-comments-responsive-671.png` 与 `design/implementation-browser-comments-responsive-619.png`。所有截图像素尺寸等于对应 CSS 视口，`devicePixelRatio = 1`，无需密度换算。
- 极窄屏补充源：用户在 `452 × 413` 浏览器视口继续标注当前主任务卡和“展开当前对象”入口；实现证据为 `design/implementation-browser-comments-responsive-452.png`，浏览器视口覆盖 `452 × 413`、扣除垂直滚动条后的内容宽度 `437px`、全页截图 `437 × 640`、`devicePixelRatio = 1`。
- 摘要卡补充源：用户在 `366 × 866` 浏览器视口标注订单摘要右侧统计文案逐字换行，并明确要求沿用正式设计稿的窄屏规则隐藏次要文案；实现证据为 `design/implementation-browser-comments-responsive-366.png`，截图像素与 CSS 视口均为 `366 × 866`、`devicePixelRatio = 1`。
- 角色切换补充源/实现：正式原型 `design/source-browser-comments-role-switch-407.png` 与生产 `design/implementation-browser-comments-role-switch-407.png`；两者均为 `407 × 866 CSS px`、截图像素 `407 × 866`、`devicePixelRatio = 1`，状态均为店员打开“切换演示角色”。用户同视口标注的修复前生产证据保存在 `design/implementation-browser-comments-role-switch-407-before.png`。

## 对比证据

### 全视图

- 时间预览：`design/source-ticket05-time-preview-1440-final.png` 与 `design/implementation-ticket05-time-preview-1440.png`。
- 时间确认最终对比：`design/source-ticket05-time-confirm-1440-final.png` 与 `design/implementation-ticket05-time-confirm-1440-final.png`。
- 重置影响最终对比：`design/source-ticket05-reset-impact-1440-final.png` 与 `design/implementation-ticket05-reset-impact-1440-final.png`。
- 生产状态证据：`design/implementation-ticket05-time-processing-1440.png`、`design/implementation-ticket05-time-success-1440.png`、`design/implementation-ticket05-time-failure-1440.png`、`design/implementation-ticket05-time-limit-1440.png`、`design/implementation-ticket05-reset-confirm-1440.png`、`design/implementation-ticket05-reset-success-1440.png`、`design/implementation-ticket05-reset-failure-1440.png` 与 `design/implementation-ticket05-old-tab-invalid-1440.png`。
- 正式原型异常状态：`design/source-ticket05-time-failure-1440.png`、`design/source-ticket05-time-limit-1440.png`、`design/source-ticket05-reset-confirm-1440.png` 与 `design/source-ticket05-reset-failure-1440.png`。

### 重点区域

- 时间确认弹窗：`design/source-ticket05-time-confirm-focus.png`（`700 × 488`）与 `design/implementation-ticket05-time-confirm-focus.png`（`700 × 466`）。
- 重置影响弹窗：`design/source-ticket05-reset-impact-focus.png`（`700 × 370`）与 `design/implementation-ticket05-reset-impact-focus.png`（`700 × 398`）。
- 两组均从同视口全图按真实弹窗边界裁取；重要标题、时间值、风险说明、影响列表与主次操作在原始密度下可直接判读，因此无需继续拆分更小区域。
- 本轮工作台正式源/生产实现全视图同屏对比：`design/qa-browser-comments-responsive-comparison-1024.png`（左侧正式 `1024 × 768` 源，右侧生产 `1024 × 768` 实现）。
- 本轮顶栏重点区域同屏对比：`design/qa-browser-comments-topbar-focus-1024.png`（左右各 `1024 × 72`）；按钮图标与文字的垂直中心可直接判读，无需继续拆分。
- 极窄主任务重点区域：`design/qa-browser-comments-452-card-focus.png`（左侧正式桌面卡片视觉源，右侧 `452px` 响应式实现）。两侧视口不同，因此只比较品牌语言、信息优先级、状态/金额/时间可读性和青柠主操作，不做像素位置等值判断；`452px` 全视图以用户标注截图与实现证据直接复核。
- 极窄摘要重点区域：`design/qa-browser-comments-366-summary-focus.png` 把正式视觉源的宽屏订单/报修摘要与 `366px` 响应式实现放入同一对比输入。两者只比较图标、标题、箭头、卡片高度和信息优先级；窄屏按正式响应式规则有意隐藏统计文案，不做横向位置等值判断。
- 角色切换弹窗同屏对比：`design/qa-browser-comments-role-switch-comparison-407.png`，左侧正式原型、右侧生产实现。两侧视口、角色、弹窗状态和像素密度一致，可直接比较弹窗宽度、单列角色卡、图标/标题/范围层级、当前角色状态和安全提示。

## Findings

- 没有仍需处理的 P0、P1 或 P2 差异。
- 浏览器标注 1–4 均已关闭：顶栏工具按钮统一为居中 `inline-flex`；`960px` 及以下自动收起检查器，主区恢复完整宽度；手动展开时检查器改为右侧覆盖层，不重新挤压队列；`720px` 及以下收紧主任务列，队列内部滚动只作为更窄视口兜底。`883px`、`671px` 与 `619px` 复核中摘要行均为 `60px` 高，正文保持 `horizontal-tb`，页面 `scrollWidth = clientWidth`。
- `452px` 补充标注已关闭：标题保持单行；筛选与带 Phosphor 侧栏图标的“展开详情”组成第二行；当前主任务按“人物/金额 → 时间/到店窗口 → 主操作”重排为 `163px` 高卡片，卡片 `scrollWidth = clientWidth = 327px`，详情按钮为 `94 × 38px`，不再竖排或裁切。
- `366px` 摘要卡补充标注已关闭：订单与报修卡在 `520px` 及以下隐藏次要统计文案，只保留图标、标题和箭头；两张卡均为 `60px` 高，内容宽度 `261px` 且 `scrollWidth = clientWidth`，没有逐字竖排、裁切或横向溢出。
- `407px` 角色切换补充标注已关闭：生产弹窗沿用正式原型的窄屏结构，外边距从 `20px` 收敛为 `12px`，四张角色卡由两列改为单列；每张卡为 `345 × 112px`，弹窗为 `383 × 780px` 且无需内部滚动，人物、门店范围与当前角色状态保持横向可读。
- 可接受的动态内容差异：正式原型用已登记的预约/订单事件表达影响密度；ticket 05 生产注册表在后续预约、订单、考勤和交接 ticket 接入前为空，因此“推进到下一事件”明确禁用、`+30 分钟` 显示“没有到期业务事件”。两侧外部契约、层级和空影响表达一致，生产没有伪造事件。
- P3：正式原型使用带分隔线的共享 `Modal` 头尾，生产沿用 ticket 04 壳层无分隔线对话框；两者弹窗宽度同为 `700px`，时间确认高度相差 `22px`，重置影响高度相差 `28px`，没有改变内容顺序、可读性或操作位置。

## 必查保真表面

- 字体与排版：两侧均使用 Noto Sans SC Variable；标题、说明、时间数字、影响标签和按钮保持相同字号层级、字重与换行密度，`700px` 弹窗内没有截断。
- 间距与布局：时间确认均采用前后双列、风险条、影响区和右下主次操作；重置均采用危险说明、四角色 `2 × 2` 影响网格和二次确认入口。生产 `1024px` 下 `bodyScrollWidth = bodyClientWidth = 1024px`，`700px` 弹窗位于 `162–862px`，底部为 `611.95px`，无裁切或横向溢出。本轮 `883px` 及以下检查器自动收起，`619px` 主任务、等待回执与摘要均完整占用主区，主任务 `scrollWidth = clientWidth = 501px`；`452px` 工具区为 `97px` 高两行布局，主任务卡内部无溢出；`366px` 摘要卡收敛为“图标—标题—箭头”三列，保持 `60px` 高；`407px` 角色切换弹窗采用正式原型的 `12px` 视口边距和 `345px` 单列角色卡。
- 颜色与令牌：炭灰/海军蓝表面、青色时间反馈、青柠主操作、琥珀安全提醒和红色重置风险均沿用选定“夜间运营控制台”方向；推进后时间统一改为青色，重置危险条统一为红色。
- 图片与资产：品牌标志继续使用正式 PNG，所有操作图标使用 Phosphor；没有占位图、手写 SVG、文本符号、CSS 图形或低清替代资产。
- 文案与内容：上海业务时间、24 小时累计上限、真实服务器时间安全边界、固定到期顺序、事务整体回滚、四角色影响、新沙箱先创建后切换、旧对象封锁与安全重试均明确可见。
- 状态与交互：真实同源 Web → Hono → 临时 Postgres 流程覆盖预览、确认、行锁等待中的处理中、提交成功、API 中断后的失败与同请求重试、数据库固定到精确 24 小时后的上限、重置成功/失败以及跨标签阻断。
- 响应式与无障碍：对话框使用语义化 `dialog`/`alertdialog`、背景 `inert`、关闭按钮初始焦点和焦点循环；`Escape` 后焦点返回原业务时间触发器。风险不只靠颜色表达，处理中使用文字与旋转图标，reduced-motion 规则把动画压缩为 `0.01ms`。

## 浏览器、API 与数据库验证

- Codex 应用内浏览器控制台：正式原型、生产 `1440 × 1024`、生产 `1024 × 768` 和旧标签的 warning/error 均为 0。
- 浏览器标注回归：应用内浏览器依次验证 `1274 × 866`、`883 × 866`、`671 × 866` 与 `619 × 866`；宽到窄切换会自动移除检查器并显示“展开当前对象”，手动展开后 `360px` 检查器以绝对定位覆盖，主区宽度保持 `811px`，收起可恢复，warning/error 为 0。自动化新增窄屏检查器回归用例并通过。
- 极窄交互回归：应用内浏览器验证 `452 × 413` 的标题、筛选、详情入口和主任务卡；展开后 `360px` 检查器继续作为覆盖层，主区保持 `365px`，页面 `scrollWidth = clientWidth = 437px`；收起可恢复，warning/error 为 0。自动化把同一窄屏用例扩展到 `452px`，检查卡片无内部溢出、按钮高度和标题单行。
- 摘要卡极窄回归：应用内浏览器验证 `366 × 866`，页面 `bodyScrollWidth = bodyClientWidth = 366px`；订单与报修摘要统计文案的计算样式均为 `display: none`，两张卡均为 `263 × 60px`（内容宽度 `261px`），`scrollWidth = clientWidth`，warning/error 为 0。自动化把同一窄屏用例扩展到 `366px`，检查隐藏状态、卡片高度和内部无溢出。
- 角色切换极窄回归：应用内浏览器在 `407 × 866` 同状态打开正式原型与生产弹窗；生产 `bodyScrollWidth = bodyClientWidth = 407px`，角色网格计算为单个 `345px` 轨道，四张卡的横坐标均为 `31px`、高度均为 `112px`，弹窗 `scrollHeight = clientHeight = 778px`，warning/error 为 0。自动化新增同视口用例，检查单列、卡宽/高度、弹窗高度、关闭交互和页面无溢出。
- 双时钟：真实业务时钟自然流逝，连续 `+30 分钟` 后顶栏和服务端结果同步变化；沙箱 `expiresAt` 不变。推进标签广播失效后，第二个已打开标签从服务端自动刷新到新业务时间，同时保留本地队列筛选输入。真实 Postgres 审计同时保存业务发生时间与不可变服务器记录时间。
- 事务失败：提交前持有沙箱行锁时生产界面稳定停留在“正在原子推进业务时间”；停止 API 后显示旧时间不变和同请求安全重试，恢复 API 后成功提交。
- 重置轮换：真实新沙箱创建后会话回到顾客标准起点，另一个已打开的标签立即显示“当前标签的旧沙箱已失效”；失败时当前角色和沙箱保持不变，恢复 API 后使用同一请求安全成功。真实 Postgres 同时验证旧沙箱重置审计与新沙箱来源事件只各写一次。
- 上限：在一次性 QA 数据库把当前沙箱固定到精确 `86_400_000ms` 累计推进后，生产只显示 24 小时上限状态，不提供继续推进入口。
- 自动化：完整 `pnpm verify` 通过，包括 61 个 Vitest 单元测试、2 个开发启动测试、34 个真实 Postgres/API 集成测试、2 个设计内容基线测试与 26 个 Chromium E2E；契约与种子套件无独立测试文件并按配置通过。Web/API/数据库/领域/契约/小程序构建、格式、lint、strict TypeScript、工作区边界、敏感数据扫描和 high 级依赖审计均通过（仅报告 1 个不阻塞门禁的 moderate 漏洞）。
- 正式原型：`npm run build` 通过，`npm run test:sites` 4/4 通过；`dist/client/index.html`、`dist/server/index.js` 与 `dist/.openai/hosting.json` 均已生成。
- 审查后复核：正式原型与生产界面统一使用规范术语“真实服务器时间”；应用内浏览器在真实生产路径确认预览和确认态均已更新，旧称谓不再出现。该文案修正不改变既有布局、状态或视觉差异结论。

## 对比历史

### 第 1 轮

- P1：推进事务成功后，父级业务时钟更新导致预览加载回调重新创建，成功态立即被“选择推进方式”覆盖。
- 修复：预览读取改为只在弹窗挂载时执行，并通过引用读取最新失效/关闭回调；真实 PostgreSQL 提交后成功态持续可见。
- 修复后证据：`design/implementation-ticket05-time-success-1440.png`。

### 第 2 轮

- P2：正式原型与生产的时间确认风险条/影响区顺序、推进后颜色和重置危险说明位置不一致；生产重置弹窗比源图高约 `107px`。
- 修复：统一为“前后时间 → 风险说明 → 影响”、推进后青色令牌和重置红色危险条；合并重复说明、收紧重置弹窗与影响卡间距。
- 修复后证据：最终时间/重置全视图和两组重点区域证据；生产重置高度收敛到 `398px`，源图为 `370px`。

### 浏览器标注响应式回归

- P1：`883px`、`671px` 与 `619px` 下固定检查器列持续占宽，分别造成主任务按钮裁切、摘要文案逐字竖排和等待回执内容裁切；P2：`1274px` 顶栏工具按钮沿行内基线排布，图标与文字垂直中心相差 `4px`。
- 修复：生产角色壳监听 `960px` 媒体查询并在进入窄屏时收起检查器；窄屏手动展开改为覆盖层；顶栏工具按钮统一使用居中 flex；队列开放受控内部横向滚动，`720px` 下收紧主任务列与顶栏轨道。
- 修复后证据：`design/implementation-browser-comments-responsive-1274.png`、`design/implementation-browser-comments-responsive-883.png`、`design/implementation-browser-comments-responsive-671.png`、`design/implementation-browser-comments-responsive-619.png`，以及两张同屏对比图。复测中顶栏图标与文字中心差为 `0px`，`619px` 主任务无内部溢出，页面无横向溢出。

### 浏览器标注响应式回归第 2 轮

- P1：`452px` 下当前主任务卡的五列最小内容宽度为 `454px`，实际卡片内容宽度仅 `291px`，主操作被推到可视区外；P2：标题工具仍保持桌面横排，“现场脉冲”逐字换行，“展开当前对象”被压成 `37 × 101px` 的竖排按钮。
- 修复：新增 `520px` 极窄断点，把标题与工具拆成两行；详情入口改为 Phosphor 图标加短标签“展开详情”，保留完整可访问名称；当前主任务使用命名网格区域重排，主操作占据卡片底部整行。
- 修复后证据：`design/implementation-browser-comments-responsive-452.png` 与 `design/qa-browser-comments-452-card-focus.png`。最终卡片 `327 × 163px`、内部无横向溢出，标题 `94 × 36px`，详情按钮 `94 × 38px`，页面无横向溢出。

### 浏览器标注响应式回归第 3 轮

- P2：`366px` 下订单/报修摘要仍保留桌面统计列，约 `261px` 的内容宽度无法容纳“图标 + 标题 + 统计 + 箭头”，统计文案被压成逐字竖排，卡片异常增高。
- 修复：沿用正式设计稿的极窄屏信息优先级，在既有 `520px` 断点隐藏摘要统计文案，并把卡片网格收敛为“图标—标题—箭头”三列；宽屏统计保持不变。
- 修复后证据：`design/implementation-browser-comments-responsive-366.png` 与 `design/qa-browser-comments-366-summary-focus.png`。最终两张摘要卡均为 `263 × 60px`，正文只显示标题且内部无横向溢出。

### 浏览器标注响应式回归第 4 轮

- P2：`407px` 下角色切换弹窗仍使用桌面双列网格，单卡仅 `145.5px` 宽；人物、范围与当前角色状态被压成逐字换行，前两张卡增高到 `265.5px`，无法快速比较四个角色。
- 修复：对齐正式原型的 `720px` 以下弹窗结构，把遮罩内边距收敛为 `12px`、弹窗内边距收敛为 `18px`，角色网格切换为单列；保留生产服务端映射说明、CSRF 安全提示和原有切换行为。
- 修复后证据：`design/source-browser-comments-role-switch-407.png`、`design/implementation-browser-comments-role-switch-407.png` 与 `design/qa-browser-comments-role-switch-comparison-407.png`。最终角色卡 `345 × 112px`，四张卡等宽等高，弹窗完整位于视口内且无需滚动。

### 最终复核

- 同角色、同时间、同视口把源图与生产截图放入同一次对比输入，字体、间距、颜色、图片资产、图标、文案、状态和操作逐项复核；P0/P1/P2 清零。
- ticket 04 及更早轮次保存在 [`design-qa-history.md`](design-qa-history.md) 和 Git 历史中。

## Ticket 19 — 店长门店资料、区域与座位配置

### 对照源与证据

- 视觉方向继续使用选定源 `design/reference/selected-night-operations-console.png`，状态源为正式原型店长“门店配置”的基本资料和区域与座位页。
- `1440 × 1024` 基本资料同屏比较：`design/qa-ticket19-profile-comparison-1440.png`；左右原图分别为 `design/source-ticket19-profile-1440.png` 与 `design/implementation-ticket19-profile-1440.png`。
- `1024 × 768` 区域与座位同屏比较：`design/qa-ticket19-seats-comparison-1024.png`；左右原图分别为 `design/source-ticket19-seats-1024.png` 与 `design/implementation-ticket19-seats-1024.png`。
- 专用表单与依赖状态：`design/implementation-ticket19-seat-dialog-1024.png`、`design/implementation-ticket19-hours-dialog-1024.png`、`design/implementation-ticket19-dependencies-1024.png`。
- `06:00` 边界后的营业规则可见性：`design/implementation-ticket19-hours-effective-1440.png`，并与相同 `1440 × 1024` 正式资料源组成 `design/qa-ticket19-hours-effective-comparison-1440.png`；生产页同时显示当前实际规则、默认回退、按适用日已生效版本和真正未来版本。

### Findings 与修复

- 首轮 P2：依赖弹窗在未关闭报修为 `0` 时仍把报修卡表现为可执行去向，可能误导店长进入无关流程。修复后零依赖卡禁用并显示“当前无报修依赖”，非零预约卡保持首焦点和可执行去向；最终状态为 `[可用, 禁用]`。
- 没有仍需处理的 P0、P1 或 P2 差异。正式原型的四个配置 Tab 继续作为完整信息架构来源；ticket 19 生产范围只接入基本资料和区域与座位，并在同一夜间运营控制台语言下补充固定门店提示、区域导航、搜索、服务端版本和依赖数量。
- P3：生产基本资料页把正式原型逐日 24 小时列表收敛为“当前实际规则 + 默认回退 + 各适用日已生效版本 + 真正未来版本”四层状态，以便直接表达服务端生效语义；颜色、字体、表面、标题层级和青柠主操作保持一致。只有工作日版本时周末仍可见默认回退；工作日/周末规则跨过上海时间 `06:00` 经营日边界后仍保留在已生效区，并由 `1440 × 1024`、`1024 × 768` 自动化和真实浏览器状态共同验证。

### 必查表面与交互

- `1440 × 1024` 下生产页面 `bodyScrollWidth = bodyClientWidth = 1440px`，资料表单与营业时间保持双列；`1024 × 768` 下页面和主区分别为 `1024/1024px` 与 `941/941px`，区域栏、座位表格和长名称没有页面级横向溢出。
- 座位、区域和营业规则均使用带标题、字段说明、错误区和明确主次操作的专用 `dialog`；座位表单在业务引用后禁用编号、区域、总部机型和排序，只保留合法停用路径。未来营业规则明确标注只影响未来，并把当前规则与待生效版本分开。
- 最终依赖弹窗在 `1024 × 768` 下为 `610 × 286.95px`，位于 `x=207px`、`y=240.52px`，底部 `527.47px`，无裁切；有效预约和未关闭报修分别显示数量和去向，零数量不会产生伪操作。
- 应用内浏览器真实沙箱完成“店长保存资料 → 服务端重新读取成功 → 切换顾客 → 三店卡片显示新工作名称、虚构城市和介绍”；营业规则保存后当前规则仍为 24 小时，跨过 `06:00` 经营日边界后默认回退继续可见、首个版本进入“各适用日已生效版本”，更晚版本继续留在“未来规则”。页面 warning/error 为 0。
- 键盘状态覆盖弹窗首字段聚焦、`Escape` 关闭、焦点返回触发器和焦点循环；自动化覆盖 `1024 × 768` 长名称、专用表单、服务端成功反馈、依赖阻断，以及 `1440 × 1024`/`1024 × 768` 下当前、默认回退、已生效、未来四层营业规则。数据库/API 覆盖跨店拒绝、门店内座位唯一、历史快照不改写、引用配置停用/归档、草稿删除、`06:00` 经营日与未来营业时间、幂等与并发版本冲突。

final result: passed

## Open Questions

- 无阻塞问题。

## Implementation Checklist

- [x] `WEB-C02` 模拟支付、七状态详情、取消、模拟退款、精确到期、爽约和原提交重试均为真实交互。
- [x] ticket 08 的 `360 × 800` 源/实现比较、真实浏览器、PostgreSQL/API、固定时钟边界与 E2E 通过，P0/P1/P2 清零。
- [x] `WEB-C01` 确认、价格展开、无券/可用/不可用/零应付、冲突失效与十分钟成功保留均为真实交互。
- [x] ticket 07 的同视口源/实现比较、真实浏览器、PostgreSQL 并发/回滚、API 幂等与 `360px` E2E 通过，P0/P1/P2 清零。
- [x] `WEB-C00/C01` 三店、时段、区域、机型、可订性、座位图与价格依据均接入真实服务端读取路径。
- [x] `393 × 852` 首页/座位图同屏比较和 `360 × 800` 未选/已选关键状态完成，固定操作区、安全区、图例与触控目标通过。
- [x] ticket 06 的真实浏览器、PostgreSQL/API、领域边界、性能和响应式自动化通过，P0/P1/P2 清零。
- [x] `WEB-G04` 两种受限推进、影响确认、处理中、成功、失败、安全重试与精确上限可操作。
- [x] `WEB-G06` 四角色影响、二次确认、创建中、成功、失败和旧标签失效可操作。
- [x] `1440 × 1024` 同状态全视图与重点区域对比完成；`1024 × 768` 无溢出或裁切。
- [x] 真实浏览器、API/Postgres、双时间审计、事务回滚、幂等重试、会话轮换和跨标签阻断完成。
- [x] 浏览器标注 1–4 的顶栏对齐、窄屏自动收起、覆盖层恢复和主区无裁切完成。
- [x] `452px` 补充标注的标题/工具两行布局、紧凑详情入口与主任务卡重排完成。
- [x] `366px` 补充标注的摘要统计隐藏、三列卡片和无竖排回归完成。
- [x] `407px` 角色切换弹窗的单列角色卡、正式边距、完整关闭交互与无溢出回归完成。
- [x] 完整仓库门禁与正式原型 Sites 包装测试通过，自动化结果已回填。

final result: passed
