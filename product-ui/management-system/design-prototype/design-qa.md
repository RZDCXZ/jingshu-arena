# 竞枢管理端设计 QA

- 状态：`current`（当前有效）
- 检查日期：2026-08-09
- 已验证契约版本：`product-ui/management-system/prod.md` 1.1
- 验证范围：[`02 — 统一跨端设计基线与合成命名`](../../../.scratch/jingshu-basic-rc/issues/02-unify-design-baseline.md)

## 对照目标

- 视觉真相源路径：`design/reference/selected-night-operations-console.png`
- 浏览器渲染实现截图：`design/implementation-ticket02-staff-workbench-1440.png`
- 视口：Codex 应用内浏览器 `1440 × 1024 CSS px`
- 源图像素：`1487 × 1058 px`，按 Lanczos 归一化为 `1440 × 1024 px`
- 实现像素：`1440 × 1024 px`
- 密度归一化：`deviceScaleFactor = 1`；源图与实现按相同像素尺寸比较
- 状态：店员角色 / 工作台 / 首条预约已确认 / 实时模式 / 业务时间 19:30
- 受命名变化影响的补充状态：总部连锁看板、门店比较筛选与图表、总部及店长门店配置、门店资料确认弹窗、人员与排班汇总

## 对比证据

- 全视图合并对比：`design/qa-ticket02-staff-workbench-comparison.png`，左侧为归一化视觉源，右侧为浏览器实现。
- 重点区域合并对比：`design/qa-ticket02-staff-workbench-focus.png`，聚焦品牌、门店上下文、导航、标题和当前预约。
- 总部连锁看板：`design/implementation-ticket02-chain-dashboard-1440.png`。
- `1024 × 768` 门店比较：`design/implementation-ticket02-store-compare-1024.png`。
- `1024 × 768` 店长门店资料与城市弹窗：`design/implementation-ticket02-manager-store-profile-1024.png`。

## Findings

- 没有仍需处理的 P0、P1 或 P2 差异。
- 客观检查：棱镜旗舰店、星桥标准店与极点新店在卡片、筛选、图例、表格、异常摘要、排班汇总和弹窗中均完整显示；栖光市在店长与总部门店资料中完整显示。`1024 × 768` 下相关文本的 `scrollWidth/clientWidth` 与 `scrollHeight/clientHeight` 检查均无裁切。
- 可接受差异：选定视觉源只展示棱镜旗舰店的店员工作台，不包含另外两家门店和城市字段；这些内容因此以同一管理端设计系统中的浏览器状态为补充证据，不对不同页面做伪像素比较。既有产品化文案、图标与密度差异没有因本次改名扩大。

## 必查保真表面

- 字体与排版：继续使用 Noto Sans SC Variable；新店名与城市字段的字号、字重、行高和字距未变，未出现截断、破坏性换行或标题密度回归。
- 间距与布局节奏：`1440 × 1024` 的顶栏、侧栏、任务队列和检查器比例保持不变；`1024 × 768` 的筛选、图表图例、配置表单和弹窗仍完整位于可用视口。
- 颜色与视觉令牌：深海军蓝、青色、青柠、琥珀和红色语义映射未改变；门店颜色索引继续保持三店并列而不生成排名。
- 图片质量与资源保真度：品牌标志继续使用仓库 PNG，界面图标继续使用 Phosphor；本次没有新增或替换图片、CSS 图形、手写 SVG 或占位资产。
- 文案与内容：正式地点统一为栖光市、棱镜旗舰店、星桥标准店和极点新店；三店仍对应 96/64/40 座、既定营业时间及主演示、交接异常、缺货与设备故障特征。人物、商品、备件、金额与事件继续为虚构合成内容。
- 状态与交互：导航、角色切换、店员预约从已确认推进到已到店/使用中、店长与总部门店资料弹窗、总部筛选和图表均可操作。
- 响应式与无障碍：`1024 × 768` 下主操作、弹窗和新名称可见；表单保留语义标签，角色与门店状态不只依赖颜色。

## 浏览器与交互验证

- 应用内浏览器控制台 `warning/error`：0。
- 角色切换：店员 → 总部运营 → 店长 → 店员均成功，并进入各自默认首页。
- 主演示：首条预约可从“办理到店”推进为“开始使用”，主演示进度从 3/12 推进到 5/12。
- 命名状态：总部连锁看板、门店比较筛选/图表/表格、门店配置下拉、人员与排班汇总及异常时间线均显示统一名称。
- 城市状态：店长门店资料、总部展示资料与店长确认弹窗均显示“栖光市（虚构）”。
- 构建与包装：`npm run build` 通过；`npm run test:sites` 4/4 通过。

## 对比历史

- ticket 02 第 1 次合并对比没有发现可执行的 P0/P1/P2，因此没有为视觉问题修改样式，也不需要第二轮对比。
- 本次改名前的已解决对比轮次与批注证据保存在 [`design-qa-history.md`](design-qa-history.md)。

## Open Questions

- 无阻塞问题。

## Implementation Checklist

- [x] 三店名称、城市、座位规模、营业时间和经营特征一致。
- [x] 受影响页面、筛选、图表、时间线和弹窗无截断或密度回归。
- [x] 管理端导航、角色切换、业务弹窗和主演示可操作。
- [x] 同视口全视图与重点区域合并对比完成。
- [x] 构建、Sites 包装与控制台检查通过。

final result: passed
