# 18 — 完整验证并恢复 1.20 设计 QA

**What to build:** 对 1.20 URL 路由变更执行完整自动化和视觉验证，并只在证据齐备后恢复管理系统正式设计参考的当前 QA 状态。

**Blocked by:** 17 — 收缩旧导航并完成四角色路由集成.

**Triage:** ready-for-agent

**State:** open

- [ ] 运行仓库规定的 `pnpm verify`、正式原型构建和 Sites 构建，全部通过且无跳过本次路由范围的失败项。
- [ ] 在 `1440 × 1024`、`1024 × 768` 和 `360 × 800` 完成顾客、店员、店长、总部关键旅程的视觉与交互 QA。
- [ ] 验证直接深链、刷新、后退/前进、复制地址、无权边界、查询参数规范化、移动端返回和脏表单保护，结果与规格一致。
- [ ] 检查控制台错误、焦点恢复、键盘操作、页面标题、横向溢出、滚动恢复和快速切换时的陈旧数据，不遗留未解释异常。
- [ ] 更新 `product-ui/management-system/design-prototype/design-coverage.md`，逐项记录本次新增路径、状态和旅程覆盖，不重复功能契约内容。
- [ ] 仅在全部门禁通过后更新 `product-ui/management-system/design-prototype/design-qa.md` 为 1.20 当前状态，并记录构建、视口、旅程和证据位置。
- [ ] 如任一门禁失败，QA 文档继续明确标为 stale/blocked，记录失败项和复现证据，不声称本次 UI 变更完成。
- [ ] 交付前没有未解决的 P0、P1 或 P2 路由/视觉缺陷，且正式原型仍可按运行指南构建和启动。

## Comments
