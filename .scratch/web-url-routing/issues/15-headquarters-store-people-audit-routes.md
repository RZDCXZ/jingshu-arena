# 15 — 总部门店配置、人员与审计路由

**What to build:** 完成总部跨店配置、人员和审计深链，以稳定门店代码承载上下文，并在服务端维持总部授权边界。

**Blocked by:** 12 — 店长门店配置路由; 13 — 店长人员、排班与审计路由; 14 — 总部看板、门店比较与连锁目录路由.

**Triage:** ready-for-agent

**State:** open

- [ ] `/hq/stores` 规范重定向到首个可见稳定门店代码的 `/hq/stores/:storeCode/profile`；不使用数组下标、显示名或客户端临时 ID。
- [ ] 总部门店资料、机位、价格和商品分别使用 `/hq/stores/:storeCode/profile`、`/seats`、`/pricing` 和 `/products`，页签由 URL 完全受控。
- [ ] 切换门店生成目标门店的规范地址；后退、前进、复制地址和刷新恢复相同门店与配置子页。
- [ ] `/hq/people`、`/hq/audit` 和 `/hq/audit/events/:auditEventId` 支持直接访问，审计列表进入详情后后退恢复原筛选。
- [ ] 无效、不可见或无权门店代码及事件 ID 显示安全边界，且不得回显该对象是否属于另一授权范围。
- [ ] 服务端验证总部角色、门店可见集合和对象授权；路径或查询参数不能扩大门店与人员数据范围。
- [ ] 配置或人员脏表单在门店切换、页签切换、浏览器后退和卸载前触发确认；取消时 URL 与草稿保持原状。
- [ ] `1440 × 1024` 和 `1024 × 768` E2E 覆盖门店选择、四个配置子路由、人员、审计详情、无权边界、焦点和横向溢出。

## Comments
