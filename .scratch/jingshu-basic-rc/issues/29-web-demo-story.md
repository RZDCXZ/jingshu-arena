# 29 — 基于业务证据的十二步 Web 主演示

**What to build:** 让一位无需陪同的访客从公开入口顺序切换四个角色，完整走完预约、订单、维修、库存、看板、导出和重置故事。

**Blocked by:** 09 — 店员办理到店、使用与完成预约; 12 — 店员商品订单履约、取消与损耗; 16 — 备件领退、解决说明与维修验证闭环; 22 — 单店经营看板与经营日下钻; 23 — 本店审计与筛选 CSV 导出; 26 — 总部三店比较、全沙箱审计与导出; 27 — 实时失效通知、轮询与只读降级; 28 — 沙箱容量、自然过期与可重试清理。

**Triage:** ready-for-agent

**State:** resolved

- [x] WEB-G03 提供可收起并重新打开的十二步清单，每步显示目标角色、业务对象、入口、完成条件、已有证据和下一动作。
- [x] 清单进度只从主演示顾客、业务对象、业务事件、库存流水、导出和重置事件推导，不能手工勾选或被背景历史提前完成。
- [x] 完整主路径从公开入口创建沙箱，在棱镜旗舰店完成竞技型两小时立即预约、预约体验券和模拟支付。
- [x] 店员办理到店和使用；顾客创建并模拟支付商品订单；店员可完成或保留已模拟支付后履约中的订单。
- [x] 业务时间推进 30 分钟后，顾客提交“耳机右声道无声”，店员分派、开始处理、领用一副替换耳机并提交解决说明。
- [x] 维修开始后预约按价格片段产生模拟退款，座位维护；店长验证成功关闭报修并恢复座位。
- [x] 店长看到单店指标和审计变化；总部看到三店比较并成功导出当前筛选 CSV；最后重置回起点。
- [x] 每个完整沙箱物化独立三店配置、最近十四个经营日历史、当前积压和主演示数据；指标、列表和导出均来自记录而非预计算常量。
- [x] 种子生成保持确定性、单事务和幂等；应用至少兼容仍可访问的旧种子 24 小时，新版本只影响新建或重置沙箱。
- [x] 任一步失败保留此前已提交合法状态并给出恢复入口；角色切换或刷新后清单恢复同一进度，重置后归零。
- [x] 使用 Product Design 插件的 image-to-code 工作流实现主演示抽屉的展开、收起、受阻、完成和下一动作，并保持与管理端原型的共享壳和视觉层级一致。
- [x] 使用 Product Design design-qa 对十二步关键帧执行同视口、同状态比较，覆盖 1440×1024 管理端、1024×768 管理适配和 360px 顾客 H5；P0/P1/P2 清零并通过。
- [x] Playwright 从全新浏览器会话完成整条故事，验证同一沙箱、事件推导进度、旧标签失效、导出内容和重置后的旧对象拒绝。

## Comments

- 2026-08-12：完成。新增服务端 `GET /api/v1/demo/story`，以受保护顾客、主演示对象、业务事件、库存流水、导出审计和重置时间推导 12 步证据；Web 抽屉仅展示服务端进度并能把访客带到对应角色和真实页面。立即预约改为选择到店窗口仍有效的最近半小时片段。`demo-story.integration.test.ts` 覆盖完整 PostgreSQL 主路径、导出、旧标签/旧对象拒绝与重置后重新开始；`WEB-G03` 覆盖抽屉、角色跳转、焦点和 360px 无横向溢出。视觉比较证据位于 `product-ui/management-system/design-prototype/design/evidence/ticket-29-web-demo-story/`，结论和受控差异记录在 `design-qa.md`。验证通过：`pnpm typecheck`、`pnpm format:check`、`pnpm lint`、`pnpm build`、`pnpm check:sensitive`、`pnpm security:audit`、管理端正式原型构建，以及 `pnpm exec playwright test --workers=4`（71 passed）。
- 2026-08-12：复验补充。`tests/e2e/demo-story-real.spec.ts` 现在包含两条真实浏览器回归：一条服务端证据回归覆盖完整主路径、旧标签 `410`、重置后旧对象 `404` 与抽屉归零；另一条由新鲜访客仅通过页面完成预约、订单、维修、单店经营/审计、三店预约 CSV 和重置，API 读取只用于断言已提交的证据。测试启动器使用隔离的 Web → Hono → 临时 PostgreSQL 服务、独立端口和 Next 构建目录并自行清理临时服务；普通 E2E 固定为 4 个 worker，避免本机五并发时的资源争用。立即预约按 `seed_version` 固化：新标准种子选择到店窗口仍有效的最近半小时片段，仍在 24 小时访问期内的旧种子继续使用创建时规则。最终 `pnpm test`、`pnpm typecheck`、`pnpm format:check`、`pnpm lint`、`pnpm build`、`pnpm check:sensitive`、`pnpm security:audit` 和管理端正式原型构建均通过。
