# 37 — 基础设施探针与生产等价预览

**What to build:** 在生产等价但非生产的资源上实测目标云拓扑，并在发布前解决 EdgeOne 运行时、数据库、图片、实时连接和回滚风险。

**Blocked by:** 36 — 全系统安全、并发、可访问性与性能门禁。

**Triage:** ready-for-agent

**State:** open

- [ ] 探针验证 EdgeOne 对锁定 Node/pnpm/Next.js 构建的实际支持，不把公开文档或本地构建成功当作平台验证。
- [ ] 同源 /api/v1 路由进入 Hono Cloud Function，其余请求进入 Next.js，不依赖 EdgeOne 不支持的 redirects 或 rewrites。
- [ ] Hono 核心使用标准 Fetch Request/Response，EdgeOne 只通过薄适配入口接入，@hono/node-server 仅用于本地开发。
- [ ] 实测 Cloud Function 的 Node 运行时精确版本、超时、请求/响应体、依赖大小、流式响应、错误处理和连接复用。
- [ ] 若仍只能使用已结束上游支持且无法确认安全维护的 Node 20，必须迁移 API 到受支持运行时，或由项目所有者显式记录并接受风险；未决时探针不得通过。
- [ ] 独立 Neon 预览环境通过空库迁移、RLS、时间排斥约束、事务、并发、连接和冷启动测试。
- [ ] 私有 Blob 直传意图、代理回退、实际图片净化、短期签名读取、孤儿文件和沙箱清理在平台资源限制内通过。
- [ ] SSE 约 25 秒短连接、重连、平台缓冲及轮询正式退路均实测，失败时产品降级状态与规格一致。
- [ ] 定时过期、数据库/Blob 清理、健康检查、结构化日志、限流和 Secret 配置在非生产资源运行。
- [ ] 建立生产等价预览、前向 expand/contract 迁移、应用回滚和重新部署上一成功构建的演练。
- [ ] 报告记录平台配置、构建版本、日期、地区、测试步骤、原始结果、限制、回滚结果和是否通过，不包含任何 Secret。
- [ ] 探针未全部通过前，文档和 UI 不声称目标运行时、图片上传、SSE、清理或大陆访问已经验收。

## Comments
