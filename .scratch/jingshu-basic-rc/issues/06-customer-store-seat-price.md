# 06 — 顾客三店浏览、座位可订性与价格预览

**What to build:** 让顾客浏览三家虚构门店，并按日期、时段、区域和机型找到一个明确可订座位及可解释的半小时价格。

**Blocked by:** 03 — 公开入口与版本化三店沙箱; 04 — 服务端角色上下文与共享演示壳。

**Triage:** ready-for-agent

**State:** resolved

- [x] WEB-C00/C01 展示棱镜旗舰店、星桥标准店和极点新店的营业时间、座位规模、区域、机型档案和演示边界。
- [x] 顾客可以选择未来七天内、半小时对齐、1–8 小时且不跨门店非营业时间的日期与时段；立即预约预览使用当前半小时片段起点。
- [x] 座位只保存正常或维护中的运营状态；可订、已预留和使用中结果按当前查询时段与预约记录推导。
- [x] 96 座旗舰店支持分区浏览、筛选、缩放或等价局部导航，点击目标可用且不会一次性压缩成不可操作的小点。
- [x] 机型档案固定体现标准型、竞技型、旗舰型及既定座位数量和体验描述，不绑定真实硬件品牌。
- [x] 价格预览按工作日/周末、跨午夜和半小时片段使用整数分计算，展示每个片段依据和总价，不出现浮点误差。
- [x] 改变时段、区域或机型会重新查询可订性；已选座位失效时清除选择并解释原因。
- [x] 使用 Product Design 插件的 image-to-code 工作流，以小程序 MP-01、MP-03 至 MP-06 的信息架构和管理端 Web 沙箱视觉语言实现顾客 H5，筛选、时间选择、座位选择、图例和返回修改均可操作。
- [x] 使用 Product Design design-qa 在 360px 顾客 H5 和旗舰店座位图关键状态下对照对应设计原型，验证字体、触控目标、图例、固定操作区和安全区，P0/P1/P2 清零并通过。
- [x] 领域和 API 测试覆盖经营日、营业时间、普通/立即预约边界、可订性推导、周末系数、跨午夜及价格片段。
- [x] 中端移动设备上旗舰店座位图首次可交互不超过一秒，筛选、缩放和选择反馈不超过 100 毫秒。

## Comments

- 2026-08-10：完成 schema v5 与 seed `2026-08-09.2`，在独立沙箱内物化三种机型、10 个区域、200 个座位、有效价格计划和用于推导状态的相邻预约；座位只存 `normal`/`maintenance`，可订、已预留和使用中均按查询时间与重叠预约实时推导。顾客目录与可订性 GET API 只接受服务端签名顾客上下文，并继续由 PostgreSQL RLS 隔离。
- 2026-08-10：完成 `WEB-C00/C01` 顾客 H5，覆盖三店选择、立即/未来七天、半小时对齐、1–8 小时、区域/机型局部导航、四列座位图、自动重查与失效选择清除、逐片段价格依据、错误重试和明确的 ticket 07 写入边界。标准/竞技/旗舰数量分别固定为旗舰店 40/40/16、标准店 32/24/8、新店 24/12/4。
- 2026-08-10：Product Design image-to-code 与 design-qa 完成。正式预约首页、座位图与生产 `393 × 852` 截图已分别进入同一比较输入，另验证 `360 × 800` 未选/已选关键状态；修复固定操作区和进度条窄屏裁切后，字体、触控目标、非颜色图例、安全区及内部宽度的 P0/P1/P2 均为零，当前报告 `final result: passed`。
- 2026-08-10：领域与真实 API/PostgreSQL 测试覆盖 06:00 经营日、普通/立即预约、非营业时间、未来七天、半小时对齐、可订性推导、工作日/周末、跨午夜和整数分价格片段。Chromium 自动化验证服务端响应后首个可操作状态小于 1 秒、本地选择反馈小于 100ms、360px 无横向溢出且所有可操作按钮不小于 44px。
- 2026-08-10：完整 `pnpm verify` 通过格式、lint、工作区边界、strict TypeScript、61 个 Vitest 单元测试、2 个开发启动测试、34 个真实 PostgreSQL/API 集成测试、2 个设计基线测试、26 个 Chromium E2E、全工作区构建、敏感数据检查和 high 级依赖审计；仅报告 1 个不阻塞门禁的 moderate 漏洞。
- 2026-08-10：双轴代码审查完成且无遗留 finding。审查中修复跨店默认机型落入空区域、无座位组合生成无意义价格计划、预览主操作缺少可见反馈，以及并发 E2E 重试计数不稳定；定向回归与最终完整门禁均再次通过。
