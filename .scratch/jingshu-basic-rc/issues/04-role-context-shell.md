# 04 — 服务端角色上下文与共享演示壳

**What to build:** 让访客在同一沙箱中安全切换顾客、店员、店长和总部运营，并始终看清当前人物、角色、门店范围和演示边界。

**Blocked by:** 03 — 公开入口与版本化三店沙箱。

**Triage:** ready-for-agent

**State:** resolved

- [x] 服务端把四个公开入口映射到固定受保护演示人物，并签发包含沙箱、角色、人物和门店范围的 HttpOnly、Secure、SameSite=Lax 会话。
- [x] API 只信任服务端角色上下文；伪造 URL、请求体或请求头中的角色、门店和沙箱不能扩大权限。
- [x] Postgres RLS 以事务内显式沙箱上下文隔离数据，缺少上下文时默认拒绝；应用权限层继续限制角色、门店和具体能力。
- [x] WEB-G02 持续显示演示数据、当前人物、业务角色、门店范围、沙箱生命周期和数据新鲜度。
- [x] WEB-G05 列出目标角色、人物和范围；有未提交表单时必须确认放弃，切换后轮换角色上下文与 CSRF。
- [x] 一个标签切换角色后，其他标签的旧角色写请求立即返回专用错误并被审计，不能靠旧缓存继续提交。
- [x] 顾客仅管理自己的记录，店员和店长仅限所属门店，总部覆盖三店但不继承一线动作；允许与拒绝矩阵均有真实 API/数据库测试。
- [x] 使用 Product Design 插件的 image-to-code 工作流实现共享演示壳、角色切换器、未提交表单确认和旧角色阻断状态，所有可见控件具有真实行为。
- [x] 使用 Product Design design-qa 对照管理端原型的默认 1440×1024 状态和 1024×768 收起导航状态，检查字体、间距、颜色、图标、文案、焦点及 reduced-motion，P0/P1/P2 清零并通过。
- [x] 键盘可以完成角色切换，焦点正确进入和返回对话框，角色与状态不只依赖颜色表达。

## Comments

- 2026-08-09 — 完成服务端角色上下文、真实权限接缝与共享演示壳。v2 签名会话持久化并校验唯一当前角色/版本，兼容滚动窗口内的 v1 会话与旧 writer；角色切换、canonical recovery、结果未知版本栅栏、CSRF/Origin、RLS、拒绝审计和四角色权限矩阵均接入真实 API/Postgres。
- 2026-08-09 — 完成 Product Design image-to-code 与 design-qa：默认桌面、1024 收起导航、角色切换、未提交输入确认和旧标签阻断完成同视口对照；焦点循环、背景 inert、reduced-motion、自然到期、启动故障禁写和 stale 重载恢复均完成浏览器验证。契约、覆盖记录与 QA 真相源已同步到管理端产品文档 1.2。
- 2026-08-09 — 验证通过：`pnpm verify`（16 个单元测试、24 组真实 Postgres/API 集成、2 个设计内容基线、16 个 Chromium E2E、全工作区构建、敏感数据检查和 high-level 依赖审计）；正式原型 `npm run build` 与 `npm run test:sites` 4/4 通过。两位独立审查者最终复审均为 clean，无 P0/P1/P2。数据库仓储拆分仅作为非阻塞的后续维护建议。
