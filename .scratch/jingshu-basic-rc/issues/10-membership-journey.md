# 10 — 会员成长、体验券与统一行程

**What to build:** 让顾客清楚看到跨三店共享的会员成长、体验券状态以及预约、订单、报修和模拟退款组成的完整个人行程。

**Blocked by:** 08 — 预约模拟支付、取消、过期与详情; 09 — 店员办理到店、使用与完成预约。

**Triage:** ready-for-agent

**State:** resolved

- [x] 每位顾客只有一份经营方级会员档案，等级为青铜、白银、黄金，门槛固定为 0、500、1500，累计成长值终身不降级。
- [x] 主演示顾客初始为白银、860 成长值，并拥有一张预约体验券和一张商品体验券。
- [x] 已完成预约按体验券减免后的最终模拟金额每满一元获得一点成长值；取消、过期和设备退款片段不重复发放。
- [x] 体验券展示可用、占用、已使用、已过期及关联交易和释放条件，不提供储值、转让、折现或手工调整。
- [x] WEB-C03 行程按当前、未来和历史聚合预约，并在关联数据存在时展示商品订单、报修和模拟退款。
- [x] 会员和行程查询只返回当前顾客数据，跨顾客、跨沙箱对象枚举被安全拒绝。
- [x] 使用 Product Design 插件的 image-to-code 工作流，以 MP-16/MP-17 的信息架构实现 Web H5 行程与会员页面；Tab、券状态、成长记录、空状态和关联对象跳转均可操作。
- [x] 使用 Product Design design-qa 在 360px 的白银会员、四种体验券状态、当前行程和空历史状态下对照小程序原型并适配 Web 沙箱标识，P0/P1/P2 清零并通过。
- [x] 领域测试覆盖等级边界、成长值取整、终身不降级、券状态与预约完成/取消/过期结果。
- [x] 页面持续说明这是演示数据，不使用“积分”“余额”“返现”或真实账户措辞。

## Comments

- 2026-08-10：完成 schema v9 与 seed `2026-08-10.4`。新增经营方级会员档案和不可变成长事件，主顾客固定白银 `860`，背景顾客各自保持唯一档案；成长、体验券、预约结果与 RLS 沙箱隔离均由真实 PostgreSQL 权威保存。
- 2026-08-10：领域规则固定青铜/白银/黄金 `0/500/1500` 门槛、终身不降级、最终模拟金额向下取整及完成幂等发放；取消、过期与退款片段不产生重复成长。预约完成时在同一事务写入成长事件并更新档案，唯一业务来源约束阻止重复发放。
- 2026-08-10：新增当前顾客专用的 `/api/v1/customer/membership` 与 `/api/v1/customer/journey`。会员返回四态券、释放条件、关联预约和成长记录；行程按当前/未来/历史聚合预约并预留订单、报修、模拟退款关联结构。非顾客、查询参数枚举、跨顾客与跨沙箱对象访问均被拒绝。
- 2026-08-10：`WEB-C03` 已按 `MP-16/MP-17` 接入真实 Web → Hono → PostgreSQL 路径：一级行程/会员导航、三组行程、历史筛选空态、白银档案、下一门槛、四态券、关联预约和成长记录跳转均可操作；页面持续显示 Web 独立演示沙箱边界，无“积分”“余额”“返现”或真实账户措辞。
- 2026-08-10：Product Design image-to-code 与 design-qa 使用应用内浏览器完成 `360 × 800` 的当前行程、历史空筛选及四态券源图/实现同屏比较；修正正式 `MP-16` 终态分组、顾客页历史空态恢复入口和内部终态代码文案。两侧控制台 warning/error 为 0，最终 P0/P1/P2 清零，证据与结论已写入两端 `design-coverage.md`、`design-qa.md` 及管理端 `design/`。
- 2026-08-10：验证通过：211 个 Vitest 单元测试、2 个开发启动测试、64 个真实 PostgreSQL/API 集成测试、2 个设计内容基线测试与 37 个 Chromium E2E；完整 `pnpm verify` 的格式、lint、strict TypeScript、构建、工作区边界、敏感数据扫描和 high 级依赖审计全部通过，仅报告 1 个不阻塞门禁的 moderate 漏洞。正式小程序原型运行时完整性检查（28 个受保护文件）与生产构建通过。
- 2026-08-10：按用户要求在同一代理内串行完成 Standards/Spec 双轴代码审查。领域术语、已接受 ADR、前向迁移、事务/RLS、产品契约和 ticket 验收逐项复核；移除未使用的 E2E 空历史夹具后，两轴均无遗留 finding。
