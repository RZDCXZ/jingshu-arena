# 27 — 实时失效通知、轮询与只读降级

**What to build:** 让同一沙箱的多个角色及时看到彼此变化，并在实时连接或后端故障时诚实降级而不伪造写入成功。

**Blocked by:** 04 — 服务端角色上下文与共享演示壳; 05 — 双时钟、时间推进与轮换重置; 08 — 预约模拟支付、取消、过期与详情。

**Triage:** ready-for-agent

**State:** resolved

- [x] 服务端以约 25 秒短连接发送沙箱级 SSE 失效通知，通知只说明资源已变化，客户端随后从 API 重新获取权威状态。
- [x] SSE 正常结束后自动重连；被缓冲、提前终止或不可用时退化为短轮询，再失败时提供手动刷新。
- [x] WEB-G02 清楚显示实时、轮询、手动、陈旧和重新连接状态，不把任何模式包装成更强能力。
- [x] 角色切换、时间推进、业务命令和重置会通知同沙箱标签；旧角色或旧沙箱标签阻止继续写入并提供刷新/表单处理路径。
- [x] 数据库或 API 不可用时 WEB-G07 使用随构建发布的标准种子快照进入只读模式，所有写按钮禁用且不切换到浏览器可写 Mock。
- [x] 写入超时不显示乐观成功，而是使用原幂等键查询或重试，并以服务端最终结果校准界面。
- [x] WEB-G08 覆盖安全请求关联 ID、429 重试时间、超时、幂等冲突、服务不可用和非法状态恢复动作。
- [x] SSE、轮询和错误日志只记录白名单安全字段，不泄露会话、请求体或内部响应。
- [x] 使用 Product Design 插件的 image-to-code 工作流实现实时状态、手动刷新、只读横幅、禁用动作、陈旧提示、旧标签阻断和错误恢复，所有可见控制均可执行。
- [x] 使用 Product Design design-qa 在管理端 1440×1024、1024×768 及 360px 顾客 H5 的实时/轮询/只读状态下对照原型，P0/P1/P2 清零并通过。
- [x] 浏览器 E2E 覆盖 SSE 重连、轮询降级、手动刷新、多标签角色变化、写入超时和只读模式不产生业务副作用。

## Comments

- 2026-08-12：完成沙箱级实时失效通道。API 使用 PostgreSQL `LISTEN/NOTIFY` 跨实例桥接，SSE 只发送静态 `{ "resource": "sandbox" }` 负载，并以约 25 秒结束连接；`/api/v1/health/realtime` 为部署探针，运行手册已补充其就绪与降级处理。
- 2026-08-12：Web 在命名连接标记缺失、提前结束或不可用时依次诚实显示重连、轮询和手动刷新；失效到达时先围栏所有写入，权威上下文确认后才解除或显示旧标签阻断。运行时 5xx 进入随构建发布的只读标准种子快照。
- 2026-08-12：正式管理端原型已补齐可执行的实时、轮询、手动、只读与恢复状态；design-qa 记录了 1440×1024、1024×768 与 360px 顾客 H5 的视觉和交互验证，P0/P1/P2 均为零。
- 2026-08-12：验证通过：`pnpm verify`（337 个单元测试、PostgreSQL 双实例集成测试、64 个 Chromium E2E、构建、敏感数据扫描及高危依赖审计）；正式原型 `npm run build && npm run test:sites` 通过（4 项）。
