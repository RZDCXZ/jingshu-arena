# 15 — 报修处理联动座位维护、预约中断与模拟退款

**What to build:** 让店员分派并开始处理报修时，座位维护、受影响预约和设备故障模拟退款以一个可信事务联动。

**Blocked by:** 08 — 预约模拟支付、取消、过期与详情; 09 — 店员办理到店、使用与完成预约; 14 — 顾客与店员创建座位报修及私有图片。

**Triage:** ready-for-agent

**State:** resolved

- [x] 新建报修必须选择本店处理人和普通、较高或紧急优先级后进入已分派；优先级只影响排序与告警，不宣称 SLA。
- [x] 只有处理人或店长可以把已分派报修推进为处理中；开始前确认明确说明座位和预约影响。
- [x] 进入处理中时，报修状态和座位维护中状态在同一事务提交，新建或已分派阶段不改变座位。
- [x] 同一事务取消从当前业务时间起该座位的待确认、已确认和已到店预约，并提前完成使用中预约；不自动换座。
- [x] 已确认但未使用预约全额模拟退款并恢复体验券；使用中设备故障保留当前不足半小时片段，退还未来完整片段且不恢复体验券。
- [x] 维护联动同时写入预约事件、报修事件、模拟退款和审计；任一失败时座位、报修和全部预约保持原状。
- [x] 顾客详情只展示公开处理动态、座位影响和模拟退款，内部分派备注、处理笔记和库存成本不可见。
- [x] WEB-S07 展示摘要、顾客描述/净化图片、座位与预约影响、内部与公开说明、事件和审计证据。
- [x] 使用 Product Design 插件的 image-to-code 工作流实现分派、开始处理、影响确认、公开/内部说明和受影响预约展示，所有按钮遵守当前状态与角色权限。
- [x] 使用 Product Design design-qa 在 1440×1024 报修详情和 360px 顾客公开动态/退款状态下对照原型，复核信息隔离、状态层级、弹窗和时间线，P0/P1/P2 清零并通过。
- [x] 真实数据库故障注入测试证明任何预约、退款、事件或审计写入失败都会使整个维修开始事务回滚。

## Comments

- 2026-08-10：完成 ticket 15。新增数据库迁移与 schema v14，实现同店分派/优先级、处理人或店长启动、座位维护、预约中断、分片退款、体验券恢复、事件/审计与幂等命令的原子联动；补齐角色安全详情 API、WEB-S07 店员队列/详情/弹窗和顾客公开影响页。真实 PostgreSQL/API 全部 106 条集成测试通过，其中维修事务 12 条覆盖五个故障注入回滚、同键并发、座位锁争用、截止结算、已过计划结束的已到店预约、客户订单释放和拒绝审计；维修 API 8 条覆盖跨店/不存在统一 404、顾客隔离与总部内部笔记隔离。其余门禁为 261 条单元测试、2 条启动测试、格式/lint/严格类型/契约/种子/设计基线/全工作区构建/敏感信息检查及 high 级别安全审计均通过（审计仅报告 1 个 moderate）。应用内浏览器在 `1440×1024` 与 `360×800` 完成真实 Web → API → PostgreSQL 旅程、同视口原型对照、信息隔离与无横向溢出验证，P0/P1/P2 为 0；管理端正式原型构建通过，小程序运行时 8/8 与构建通过。产品契约更新为管理端 v1.7/小程序 v1.3，设计覆盖与 QA 证据已回填；实现遵循 `CONTEXT.md`、ADR-0006/0012/0018 与 RC 规格，未改变已接受领域决策。双人代码复审最终无阻塞发现。
